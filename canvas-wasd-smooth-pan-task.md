# 画布 WASD 平滑平移任务

## 目标

- 按住 `W/A/S/D` 时以稳定帧率连续移动画布视角。
- 松开方向键或窗口失焦时停止移动，并只保存一次最终视口。
- 输入框、下拉框和可编辑内容继续接收正常文字输入。
- 对角移动归一化，避免同时按两个方向时速度变快。

## 验收

- 方向向量、对角速度、反向抵消和长帧限幅都有单元测试。
- WASD 定向测试及生产构建通过。
- `molimama.vip` 真实画布按住与松开按键行为符合预期。

## 边界

- 不修改鼠标平移、节点拖拽、快捷键选择或其他画布行为。
- 部署只包含本任务修改的前端文件与对应构建产物。

## 验证记录（2026-08-03）

- 定向测试：`18/18` 通过。
- 生产构建：通过。
- 完整前端测试：`501/507` 通过；6 个失败均为 `origin/main` 已存在的画布结构断言漂移。
- 生产 release：`/opt/moli-drama/releases/canvas-wasd-smooth-afb6e91-20260803T191042CST`，健康检查及公开资源检查通过。
- 线上画布重复触发 `W` 的 Y 坐标依次为 `8.32`、`16.848`、`25.5434`、`33.8634`、`42.1834`、`50.9194`，确认已由 56px 跳步改为约 8.3px 的小步连续响应。
- 松键后两次采样坐标保持一致，停止逻辑生效；浏览器测试页已关闭。

## 速度再次翻倍验证（2026-08-03）

- 按用户反馈，将代码中的平移速度从 `900px/s` 精确翻倍到 `1800px/s`；`900px/s` 中间版本未部署。
- 平滑算法、对角归一化、`32ms` 长帧限幅和停止保存逻辑保持不变。
- 定向测试：`18/18` 通过；生产构建通过。
- 完整前端测试：`501/507` 通过；仍为上述 6 个基线结构断言失败，没有新增失败。
- 生产 release：`/opt/moli-drama/releases/canvas-wasd-speed-1800-7e55b99-20260803T201823CST`，服务状态及正式域名健康检查通过。
- 公开资源 `DramaCanvas-CWt4ocFx.js` 返回 `200`，公网文件与服务器构建产物 SHA-256 均为 `53971bbc75f86f47b674147d906b2a3fc598e41567ea355cae21b83ae9c20e8d`。
- 线上画布单次 `W` 首帧位移为 `28.8px`，符合 `1800px/s × 16ms`；`S` 返回原位。横向帧间隔实测为 `17.5ms`，`D` 位移 `31.5px`、`A` 回退 `28.8px`，确认逐帧时间平滑计算仍生效。
- 浏览器重载后视口恢复为 `translate(0px, 0px) scale(0.75)`，控制台无错误，测试页已关闭。
