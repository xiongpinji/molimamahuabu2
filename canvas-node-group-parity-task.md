# 画布节点打组、整组执行与解组升级任务

## 目标

在独立画布中提供与 Neodomain 等价的核心分组交互：

1. 框选或多选至少 2 个自由节点后，在选区上方显示上下文工具栏和节点数量。
2. 点击“打组”后创建可见组框，并自动选中新组。
3. 拖动组框时，组内节点实时保持相对位置一起移动。
4. 点击“整组执行”时，仅按组内连线拓扑执行组内自由节点，不扩散到组外节点。
5. 点击“解组”后删除组框，保留成员当前位置与连线。
6. 打组、整组移动、解组均进入撤销/重做历史并持久化，刷新后组关系仍存在。
7. 单独移动组内节点后，组框自动重新包围全部成员；向外移动时扩张，向内移动时收拢，成员坐标不被二次改写。

## Neodomain 实操记录

- 反向拖拽框选可以一次选中 3 个节点，选区上方显示“3 个节点”的浮动工具栏。
- 打组会新增一个有标题、成员边界和选择态的组容器。
- 拖动组标题时，成员节点保持相对位置整体移动。
- 解组会删除容器，但成员节点停留在解组时的位置。
- 组选择工具栏包含组级操作；本项目按用户要求补充真实“整组执行”入口。
- 实操结束后已连续撤销，确认目标画布恢复为 6 个原节点、0 个分组、0 个选中节点。

## 当前代码审计

项目已有基础分组模型、持久化和底部工具栏入口，但存在以下差距：

- 打组入口混在底部工具栏中，中等宽度下文字会被视觉隐藏，不符合选区就近操作。
- 创建组后没有自动选中新组，组级执行和解组入口不会立即出现。
- 组拖动期间成员只在松手后跳到新位置，不是实时整体移动。
- “整组执行”使用了向下游扩散模式，可能运行组外节点。
- 打组和解组没有完整进入画布撤销历史。
- 单独拖动组内成员时没有重新计算组框几何，成员可以移出组框且刷新后仍保持错误边界。

## 非目标

- 不复制 Neodomain 私有源码、品牌图标或素材。
- 不改造经典分镜工作流分组。
- 不增加嵌套组、组内素材打包或组框缩放；本次只实现用户明确要求的打组、整组执行、解组及其必要交互。

## 验收

- 单元测试覆盖组持久化、组内执行边界、上下文工具栏和历史接入。
- 浏览器回归覆盖：多选 → 打组 → 单独移动成员并自动扩张组框 → 组框整体移动 → 整组执行入口 → 解组 → 刷新持久化。
- 前端构建通过；PR CI 全绿。
- 合并后完成生产备份、隔离验证、部署及真实线上浏览器复验。

## 本地验证记录

- 真实反向拖拽框选端到端用例通过，未再出现多选坍缩为单节点。
- 分组专项单元测试 12/12 通过。
- 生产前端构建通过，仅保留项目原有的大分块提示。
- 全量 Node 测试 470 项通过 464 项；6 个静态源码断言失败均可在最新 `origin/main` 的干净临时工作树复现，本次改动未触及对应画布偏好和节点编辑逻辑。
- 全量独立画布浏览器套件仍有 2 个既有失败：旧右键菜单定位与多结果素材请求计数；本次新增分组闭环用例通过。
