# 项目素材节点可用性任务

## 用户目标

画布中的图片、视频素材不能只作为预览卡片存在：保留它们时，必须能够像正式节点一样拖动、连线，并作为下游生成节点的参考输入；否则才删除无用显示。

## 当前判断

- 项目素材已经被画布数据适配器创建为 `canvasProjectAsset`，并且下游生成会读取手动连线的上游素材 URL。
- 当前素材组件缺少可见的参考输出端口，图片和视频也没有关闭浏览器原生拖拽，因此用户无法稳定地把它当成正式节点操作。
- 该节点已有预览、复制引用、指派分镜和下游参考输入消费者，不属于无用功能；本轮保留素材数据，只补交互闭环。
- 当前生成服务只消费图片参考；因此图片节点提供真实下游连线，视频/音频节点保留拖动、预览、复制引用和指派能力，不暴露无效端口。

## 验收标准

1. 图片、视频、音频项目素材在独立画布和项目画布中都成为可拖动的正式节点。
2. 图片素材节点显示右侧参考输出端口，可以创建并保存到下游生成节点的手动连线；视频/音频不伪造当前服务无法消费的连接。
3. 图片、视频、音频预览不会触发浏览器原生文件拖拽；从节点主体拖动仍可移动节点。
4. 图片连接到下游生成节点后，生成参考输入能够读取素材 URL，并兼容缺失或大小写不一致的图片类型。
5. 预览、复制引用、指派分镜功能不回归，不删除任何底层素材。

## 验证

- 新增回归先失败后修复，最终 4/4 通过；覆盖节点端口、媒体原生拖拽、项目素材 URL 和异常图片类型兼容。
- 相关画布回归 67/67 通过。
- 真实 Chrome（1440×900、1024×768）验证：
  - 从图片预览区域拖动素材节点后位置发生变化并写入布局；
  - 点击图片素材输出端口和视频节点输入端口后创建 1 条边并保存；
  - 刷新后连线保留，下游参考区显示 `1/1 已就绪`，不再显示“等待图片”；
  - 复制引用显示成功提示。
- 生产构建通过。
- 全量前端测试文件 61 个通过；2 个既存测试文件仍有 3 条与本任务无关的失败：
  - `canvasInteractionEntrypoints.test.js`：2 条；
  - `standaloneCanvasNodeEditorParity.test.js`：1 条。
- 已从最终分支移除并行带入的连线/节点编辑器提交；其触发的 4 条 CI 失败在独立端口复测后 4/4 通过。
- 双轴代码审查的规范问题已修正，规格边界问题已补回归和浏览器证据。
