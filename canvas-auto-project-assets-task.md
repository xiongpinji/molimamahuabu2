# 画布自动素材卡清理任务

## 目标

图片、视频等生成结果继续保存到“我的资产/生成历史”，但不再自动投影为画布上的“图片素材 / 视频素材 / 音频素材”卡片。

## 边界

- 不删除资产数据库记录或媒体文件。
- 不改变生成节点本身的预览、下载和历史记录能力。
- 普通素材库上传、导演截图和生成/分析结果不自动出现在画布。
- 只有用户在画布内明确执行“选择素材、上传、粘贴”时，才创建素材卡。
- 只修改资产投影判断与画布显式加入素材的标记逻辑，不改相邻功能。

## 验收标准

1. 自动生成、普通素材库上传和导演截图均不投影素材卡。
2. 画布内明确选择、上传、粘贴的素材仍可投影。
3. 独立画布和项目画布共用同一过滤规则。
4. 相关单元测试、前端全量测试与构建通过；既有基线失败不增加。
5. 线上项目 41 的 10 个自动/普通投影素材卡全部消失；总卡片从 11 个降为 1 个，仅保留用户明确从画布素材库加入的素材，资产记录数量不变。
6. 部署只覆盖本任务修改的运行时文件，并保留可回滚版本。

## 本地验证

- 素材投影专项测试：5/5 通过。
- 画布上传/粘贴入口回归：1/1 通过。
- 前端全量测试：509 项，503 通过；6 项为改动前已存在的画布静态入口基线失败，本任务未新增失败。
- 生产构建：通过（Vite 1821 个模块完成构建）。
- `git diff --check`：通过。

## 线上验证

- PR #120 的依赖门禁、构建冒烟和画布浏览器回归均通过。
- 保护式发布版本：`/opt/moli-drama/releases/canvas-assets-1462543-20260803T211327CST`。
- 只向候选版本上传了 `frontweb/src/utils/canvasAssetProjection.js` 和 `frontweb/src/views/DramaCanvas.vue`，其余文件沿用切换前的线上版本。
- 正式域名加载新构建 `DramaCanvas-2XF5k9Zy.js`；服务与 `/health` 均正常。
- 项目 41 的画布素材卡从 11 个降为 1 个；剩余项为资产 543“原始森林中的藤蔓灌木丛”，分类 `canvas-library-pick`、来源 `canvas_asset_picker`，属于用户明确加入画布的素材。
- 项目 41 的资产记录发布前后均为 69 条。
- 发布前 SQLite 在线备份位于 `/opt/moli-drama/shared/backups/pre-canvas-assets-1462543-20260803T211327CST/drama_generator.db`；原库与备份 `quick_check` 均为 `ok`，且资产数均为 69。
