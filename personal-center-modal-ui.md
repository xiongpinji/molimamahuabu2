# 个人中心弹层与去卡片化 UI 改造

## 用户问题

- 当前个人中心是独立路由页，进入后看不到原页面，也缺少明确关闭/返回入口。
- 导航位于右侧窄栏，与常见个人中心的信息浏览方向相反。
- 指标、资料和作品大量使用独立卡片，层级碎片化。

## 交互决定

- 账户入口改为打开全局大尺寸弹层，底层路由页面保持挂载，关闭后仍停留在原画布或项目页。
- 保留 `/personal-center` 独立路由作为旧链接兼容入口，但增加明确关闭/返回操作。
- 弹层使用现有 Element Plus 对话框能力，保留焦点管理、Esc 关闭和遮罩语义，不引入新依赖。
- 个人中心导航固定在左侧；移动端仍保持左侧窄栏，不改成底部导航。

## 视觉原则

- 使用一个完整工作面板，不堆叠多层卡片。
- 数据指标通过留白、分隔线和排版建立层级。
- 内容区只保留必要分组线；作品改为纵向列表，不再使用卡片网格。
- 延续现有深色界面与橙色强调色，不改全局主题。

## 成功标准

- 从账户入口打开个人中心时，当前 URL 和底层业务页不变。
- 点击关闭、遮罩或按 Esc 可关闭并回到原页面。
- 桌面端和移动端导航均位于左侧。
- 真实账户、积分、工作区、作品、安全、兑换和设置能力保持不变。
- 接口失败仍明确显示加载失败，不回退为假零值或假空数据。
- 定向单测、个人中心 E2E、生产构建、PR CI 和线上真实登录验收全部通过。

## 隔离边界

- 只修改全局个人中心入口、个人中心容器与对应测试。
- 不修改后端接口、数据库、积分语义、画布节点或图片编辑功能。

## 本地验证记录（2026-07-30）

- 定向 Node 回归：35/35 通过。
- 个人中心 Playwright E2E：6/6 通过，覆盖原页面保留、显式关闭、作品导航、核心模块、窄屏导航和接口失败隔离。
- 生产构建：`npm run build` 通过。
- 真实浏览器视觉审计：1440×900 与 390×844 均通过；已复核正常数据态、失败态、Esc 关闭、左栏滚动和底部退出入口。
- 前端全套 Node 测试：451 条中 448 条通过；3 条既有失败位于画布交互与节点编辑器测试，本任务未修改对应测试或运行时代码。
