# 用户个人中心改造任务

## 目标

依据 2026-07-30 对 `xwow.neodomain.cn` 已登录账户侧栏的实测，为茉莉妈妈增加独立个人中心，并把现有账户、积分、用量、作品和安全能力统一到一个入口。

## 参考站实测模块

- 个人信息
- 积分账单、用量统计、赚取积分
- 社群课程、礼品卡、优惠券、宝箱
- 我的作品、我的点赞、站内消息
- 发票管理、登录设备、切换账号、体验设置、退出

## 本阶段实现范围

1. 新增 `/personal-center` 独立路由和右侧纵向导航，视觉与交互参考实测站，但不复制其私有素材、品牌或代码。
2. 个人信息：展示当前用户、角色、工作区和账户状态；复用现有修改密码与退出登录能力。
3. 积分账单：接通真实余额、冻结、已用积分与积分流水。
4. 用量统计：基于真实积分流水汇总调用次数、消耗积分和模型分布。
5. 赚取积分：接通现有兑换码能力。
6. 我的作品：接通当前用户有权限访问的项目列表并可进入项目。
7. 登录与安全：展示当前会话和真实登录审计记录；不伪造多设备管理。
8. 体验设置：提供真实生效并持久化到本机的界面主题与减少动效设置。
9. 现有 `AccountBadge` 改为头像/余额入口，个人中心内继续保留工作区切换。

## 暂不伪造

当前源码没有真实数据模型或业务链的模块只展示“尚未开放”，不创建假数据：

- 社群课程、礼品卡、优惠券、宝箱
- 我的点赞、站内消息
- 发票管理
- 远程踢出其他登录设备

## 隔离边界

- 仅修改个人中心、账户入口、必要的只读 API 与测试。
- 不修改图片节点、画布编辑、视频/音频生成和模型配置。
- 不引入 GPU 依赖。
- 不改变现有登录令牌、积分结算和租户隔离语义。

## 验收门禁

- 个人中心路由必须要求登录。
- 所有金额、流水、作品和审计记录必须来自真实后端接口。
- 普通用户不能读取其他租户或用户的数据。
- 修改密码后必须清除会话并回到登录页。
- 退出登录必须清除会话。
- 桌面和移动布局可用。
- 前端行为测试、后端相关测试、生产构建与浏览器 E2E 通过。
- 双轴审查通过后才允许提交 PR 和部署。

## 本地验证记录（2026-07-30）

- [x] 前端个人中心、登录守卫与积分展示行为测试：10/10 通过。
- [x] Playwright 桌面真实数据模块与移动端待开放模块：2/2 通过。
- [x] 前端生产构建：通过。
- [x] 后端账户、积分、审计、租户上下文和项目隔离回归：29/29 通过（含新增租户上下文用户级审计隔离测试）。
- [x] `git diff --check`：通过，仅有仓库既有的 Windows 换行提示。
- [x] 标准与需求双轴初审：发现的文件命名、作品类型、审计隔离、入口收敛和会话文案问题均已修复并回归。
- [x] 双轴复审：标准轴与需求轴均无残留问题。
- [ ] PR CI。
- [ ] 线上部署与部署后冒烟验证。
