# 个人中心角色功能补齐计划

## 问题

- 普通用户个人中心把兑换入口命名为“赚取积分”，没有明确呈现“兑换码兑换”。
- 管理员从画布打开个人中心后，只能看到普通用户功能，无法进入现有管理后台。

## 根因

- 兑换接口和表单仍然存在，缺失的是清晰、可发现的入口名称。
- 管理后台页面、路由和服务端权限仍然存在，但个人中心弹层没有迁入原有的角色导航。

## 修改范围

- 保留现有个人中心弹层和左侧导航。
- 将普通用户入口明确命名为“兑换码兑换”，继续调用现有兑换接口。
- 按现有 RBAC 规则显示管理入口：
  - 平台管理员：工作区与积分、账号与权限、运营与计费、模型配置。
  - 兑换码管理员：运营与计费。
  - 运营、客服、只读人员：工作区与积分、账号与权限。
  - 普通用户：不显示管理后台入口。
- 管理入口复用现有真实后台路由；从弹层进入时先关闭弹层，再导航。

## 不修改

- 不修改积分规则、兑换码规则和服务端接口。
- 不扩大任何角色权限。
- 不修改数据库结构或生产数据。
- 不重构现有管理后台。

## 验收标准

1. 普通用户能在个人中心看到“兑换码兑换”，提交后余额刷新。
2. 普通用户看不到管理后台入口。
3. 各管理角色只看到现有 RBAC 允许的入口。
4. 平台管理员能看到全部四个管理入口，并能从弹层进入真实后台页面。
5. 本地单元测试、浏览器测试、生产构建和 PR 门禁通过。
6. 部署前完成数据库备份，部署后使用真实登录账号在线验收。

## 本地验证记录

- 个人中心专项测试：8/8 通过。
- 个人中心、RBAC、兑换码后台、计费后台和路由守卫联合测试：33/33 通过。
- 生产构建：通过。
- Playwright 真实浏览器验证：
  - 平台管理员首屏显示四个管理入口，并可进入 `/billing-admin`。
  - 普通用户不显示“管理后台”，可进入“兑换码兑换”并提交真实请求格式。
  - 兑换请求体为 `{"code":"MOLI-LOCAL-TEST"}`，模拟成功后余额即时刷新，控制台无错误。
- 全量前端静态测试：450/453 通过；3 个失败均在本次未修改的画布文件中，属于基线既有断言漂移：
  - 节点拖拽停止后立即刷新布局缓存并同步视口。
  - 右键空白画布提供 LibTV 式添加节点入口并使用点击位置。
  - 四类节点编辑器暴露 LibTV 核心参数且不隐藏在假配置按钮后。
