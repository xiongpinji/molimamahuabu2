# 茉莉妈妈短剧制作平台：第一轮真实链路验证

验证日期：2026-07-13

## 验证目标

使用真实短剧样例验证：剧本保存、角色提取、场景提取、分镜生成、首尾帧图片和模型配置链路。

## 测试项目

- 项目：`茉莉妈妈链路验证-雨夜来信`
- 剧集：`第1集：雨夜来信`
- 比例：16:9
- 剧本：3 个场景，林茉、小雨等角色

## 已通过

- 项目创建与手工剧本保存。
- 推理文字模型连接测试及真实生成。
- 自动提取 2 个角色：林茉、小雨。
- 自动提取 3 个场景：旧城区公交站、茉莉花店、家中儿童房。
- 自动生成 8 个分镜及视频提示词。
- 首尾帧模式成功启用，每个分镜出现独立首帧、尾帧双槽。
- 文生图、视频配置的密钥与网络连通测试通过。

## 已发现问题

1. 用户输入分镜数量 4 后，平台仍按时长估算生成 8 镜；需要检查输入提交与后端约束传递。
2. 模型文件中的显示名 `GPT Image 2` 不能直接作为接口模型标识；供应商实际支持 `gpt-image-2`，配置已修正。
3. 修正模型名后，图片任务进入 `processing`，最终因远端 `socket hang up` 失败；需要为长耗时图片请求增加合理超时、状态轮询或安全重试机制。
4. 配置页“测试连接”只验证密钥和网络，不验证模型名及实际生成协议，容易产生假阳性。

## 当前结论

- 文本生产链路：可用。
- 角色、场景、分镜结构：可用。
- 首尾帧产品结构：存在且可进入真实调用。
- 图片真实生成：受远端连接稳定性阻塞。
- 视频真实生成：尚未执行；应在首帧图片链路稳定后验证，避免无效调用。

## 下一步成功标准

1. 图片长任务不因单次 HTTP 连接中断而永久失败。
2. 第 1 镜首帧和尾帧均成功写回并可预览。
3. 使用成功首帧执行一段 5 秒视频生成，并记录远端任务状态和最终视频 URL。
4. 修复分镜数量约束后，输入 4 必须生成 4 镜。

## 2026-07-13 修复记录：分镜数量

- 根因：数量只进入提示词；模型返回数量不符时，后端仍直接保存结果。
- 修复：后端对用户填写的目标数量执行精确校验。首次结果不符时只允许一次重新整理；校正结果仍不精确则任务失败，不保存错误数量。
- 数据保护：进入校正前清除首轮错误的增量结果，避免校正失败后把 8 镜误报为“部分成功”。
- 自动验证：新增 2 条数量约束回归测试；后端完整测试共 35 条全部通过，语法检查和差异检查通过。
- 待真实复验：在界面填写 4，再次调用文字模型，确认最终数据库和页面均为 4 镜。

### 真实复验结果

- 页面输入值：4。
- 模型首轮实际返回：5 个分镜。
- 后端任务进度明确进入：`模型返回 5 个分镜，正在校正为 4 个...`。
- 最终任务：`completed`、进度 100、错误为空、结果总数 4。
- 数据库：第 1~4 镜共 4 条有效记录，无错误数量的活动记录。
- 页面刷新后：只展示分镜 1、2、3、4，共 4 个。
- 结论：成功标准“输入 4 必须生成 4 镜”已通过真实模型链路复验。

图片 `socket hang up` 尚未宣称修复：证据表明是供应端在约 122 秒主动断开，并非本地 10 分钟超时。由于图片请求可能已在供应端计费，暂不加入无幂等保障的自动重试。

## 2026-07-13 修复记录：图片长响应第一阶段

- 根因判断：当前供应商仅验证到同步 `/images/generations`，没有可用的异步提交/轮询接口；失败发生在约 122 秒，早于本地 10 分钟超时，符合上游同步连接被关闭的表现。
- 修复：仅对 OpenAI 兼容的 `gpt-image-*` 请求设置 `output_format=jpeg`、`output_compression=85`，减少 base64 响应体和编码/传输时间。
- 格式一致性：JPEG base64 回包写成 `data:image/jpeg;base64,...`，不再错误标记为 PNG。
- 计费保护：没有加入自动重试；网络断开属于结果未知，自动重发可能造成重复生成和重复扣费。
- 第一阶段自动验证：新增 2 条回归测试；当时后端完整测试共 37 条全部通过，语法检查通过。
- 第一阶段真实复验：只提交了 1 次第 1 镜首帧任务（图片记录 3）。请求已带 `output_format=jpeg`，但在 126.6 秒收到供应端 HTTP 524，任务失败且无本地图片；没有重试。结论：仅压缩回包不能解决供应端推理超时。
- 第二阶段修复：`gpt-image-*` 的项目尺寸映射到模型支持的较小档（横图 `1536x1024`、竖图 `1024x1536`、方图 `1024x1024`）；用户未指定质量时使用 `low` 快速档。生成成功后仍由既有本地流程适配回项目目标画幅。
- 第二阶段自动验证：新增尺寸与质量策略回归测试；后端完整测试共 38 条全部通过，语法检查通过。
- 第二阶段真实复验：图片记录 4 使用 `1536x1024 + low + JPEG` 请求成功，远端耗时约 140 秒；本地保存为 JPEG 并适配到项目目标 `2560x1440`，文件存在且任务状态为 `completed`。
- 复验数据边界：本次沿用了修复分镜数量前的旧分镜 7；该分镜已被软删除，当前活动第 1 镜为 19，且内容指纹不同。因此绑定逻辑按设计没有把旧镜头图片写入新分镜。不得将图片 4 冒充当前第 1 镜首帧，也不得未经确认再次调用付费接口。
- 结论：`gpt-image-2` 长响应生成链路已真实跑通；当前活动第 1 镜首帧仍需另行生成一次才能完成页面绑定验证。

### 当前活动第 1 镜复验

- 用户授权后，仅为活动分镜 19 提交 1 次首帧任务：图片记录 5、任务 `73441774-5e32-4e3f-b767-f6aded55ca35`。
- 请求参数已确认使用 `1536x1024 + low + JPEG`，提示词来自活动分镜 19，自身状态非删除。
- 结果：约 121 秒后失败，错误为 `图片生成网络请求失败: socket hang up`；无本地文件、无首帧绑定，未自动重试。
- 生产结论：同一快速参数曾成功一次、随后又在约 2 分钟断连，说明该供应商同步接口存在间歇性网关/连接不稳定。当前实现的缩图与快速质量档只能降低风险，无法提供收费平台所需的可靠交付保证。
- 下一修复方向：接入具有“提交任务 + task_id + 状态轮询”的异步图片供应商，或要求当前供应商提供异步查询接口/更长网关超时；在此之前保持人工确认重试，禁止自动重发结果未知的付费 POST。

## 2026-07-13 异步图片生产链路准备

- 代码审计：仓库已内置可灵 Kling 图片异步提交与轮询，配置页也已有 `kling-image` / `kling-omni-image` 选项，无需新造供应商协议。
- 精准修复：将可灵轮询 URL 构造和任务状态解析抽成可测试函数，网络调用复用同一逻辑；未改变请求参数、4 秒轮询间隔或 60 次上限。
- 自动验证：新增 2 条可灵异步契约测试，覆盖默认/自定义查询地址、任务 ID 编码、成功/失败/处理中状态；后端完整测试 40 条全部通过，语法检查通过。
- 当前阻塞：数据库中仍只有 `rehdasu.cn` 同步图片配置，没有可灵或其他异步图片供应商的密钥配置。未擅自创建空配置，也未调用外部接口。
- 下一动作：获得可灵图片 API Key（或其他明确支持异步查询的供应商参数）后，在配置页新增 `image` 与 `storyboard_image` 两条配置，先执行连接测试，再仅生成一次活动分镜 19 首帧验证完整绑定。

## 2026-07-13 Seedance 2.0 视频真实链路

- 协议根因：`shiping.djpsd.com` 不是 OpenAI `/v1/videos` 协议。供应商公开接口为 `POST /api/v1/video-jobs`，任务查询为 `GET /api/v1/video-jobs/{task_id}`；创建密钥位于请求体，查询密钥位于 `api-key` 请求头。
- 无效调用审计：第一次按 OpenAI 协议调用立即返回 HTTP 404；切换专用协议后，9 秒参数被供应商业务层拒绝。只读任务列表确认两次均未创建供应商生成任务。
- 时长适配：供应商仅接受 5、10、15 秒。为避免截断 9 秒分镜，将 9 秒向上适配为 10 秒。
- 实际生成：本地视频记录 9、供应商任务 83047，16:9 纯文本生视频；任务完成并自动下载。
- 文件验收：`vg_9_5929eb91.mp4` 存在，10,303,845 字节；H.264 视频 + AAC 音频，2560×1440，24fps，实际时长 10.05 秒。
- 绑定验收：视频记录 9、异步任务和活动分镜 19 均写入同一 `video_url` / `local_path`；视频任务无错误。分镜原有图片失败错误仍保留，未被视频流程擅自清除。
- 自动验证：新增 4 条 DJPSD 协议测试，覆盖地址归一化、请求体与时长适配、任务号解析、完成/失败/处理中轮询；后端完整测试 44 条全部通过，语法与差异检查通过。
- 结论：Seedance 2.0 的“创建任务 → 轮询 → 下载 → 分镜回写”真实链路已跑通；当前活动首帧图片链路仍是独立未解决项。

## 2026-07-13 Seedance 2.0 配置与防重复提交

- 配置入口：视频配置页新增 `DJPSD / Seedance 2.0` 正式预设，自动填写专用协议、服务地址、提交地址和任务查询地址。
- 时长提示：界面明确提示供应商仅支持 5 / 10 / 15 秒，其他时长由后端向上适配。
- 安全连接测试：DJPSD 的“测试连接”改为只读查询任务列表，不再误用 OpenAI 聊天接口，也不会创建付费视频任务。
- 防重复提交：同一分镜已有 `pending` 或 `processing` 视频任务时，生成接口直接返回现有记录并标记 `reused: true`，不重复请求供应商；已完成或已失败的记录不阻止用户主动重新生成。
- 自动验证：新增 3 条回归测试，覆盖只读连接测试、活动任务复用及完成/失败后的重新生成条件；后端完整测试 47 条全部通过。
- 前端验证：生产构建成功；本轮没有执行任何付费模型生成。

## 2026-07-13 可灵异步图片官方鉴权修复

- 根因：可灵图片预设已存在异步提交与轮询，但图片客户端只接受单一 Bearer `api_key`；配置页的官方 AccessKey / SecretKey 仅对 Omni 视频开放，官方图片账号无法完整配置。
- 后端修复：图片与分镜图片支持从配置 `settings` 读取官方 AccessKey / SecretKey，按官方 HS256 规则生成短期 JWT；中转站单一 Bearer Key 的原有用法保持不变。
- 配置修复：可灵图片和分镜图片配置现在显示、保存并重新载入 AccessKey / SecretKey；选择预设时自动填写提交与查询地址。
- 安全连接测试：改为 `GET` 查询不存在的任务编号验证鉴权，不再向图片生成接口发送付费 `POST`；路由允许 AK/SK 在不填写单一 API Key 时完成测试。
- 自动验证：新增 3 条回归测试，覆盖 JWT 生成、AK/SK 凭据识别和只读连接请求；后端完整测试 50 条全部通过，前端生产构建成功。
- 当前边界：本机未配置可灵官方 AccessKey / SecretKey，本轮没有调用任何付费图片生成，也未宣称真实供应商连接已通过。
- 用户范围确认：可灵不作为本项目当前测试供应商，不执行连接或生成。后续真实验证仅使用用户提供的 `GPT-5.5`、`gpt-image-2` 与 `seedance 2.0` 模型组；可灵代码仅作为未启用的兼容能力保留。

## 2026-07-13 GPT Image 2 防重复提交

- 保护范围：同一 `storyboard_id` 与同一 `frame_type` 已存在 `pending` 或 `processing` 图片任务时，创建接口直接返回现有图片记录并标记 `reused: true`。
- 首尾帧边界：同一分镜的首帧与尾帧按不同 `frame_type` 判断，首帧处理中不会阻止尾帧正常创建。
- 主动重试边界：`completed` 与 `failed` 记录不参与拦截，用户仍可主动重新生成。
- 扣费保护：重复判断发生在创建本地异步任务和供应商请求之前；本轮没有调用 `gpt-image-2` 或任何其他外部模型。
- 自动验证：新增 3 条回归测试；后端完整测试 53 条全部通过，语法检查与差异格式检查通过。

### 前端提交双重保护

- 首帧、尾帧、普通分镜图和“一键生成首尾帧”按钮在对应任务运行时显式禁用并显示 loading。
- 生成函数入口增加原子式本地提交锁；即使极快双击绕过按钮渲染，第二次调用也会立即返回并提示“正在生成，请勿重复点击”。
- 首帧与尾帧使用独立锁，互不阻塞；请求结束后在 `finally` 中释放，失败后仍可再次操作。
- 自动验证：前端新增 2 条提交锁测试，与原有测试合计 4 条全部通过；生产构建成功。本轮外部模型调用为 0。

### 刷新恢复与 Rehdasu 无扣费连接检测

- 刷新恢复审计：项目加载后会查询当前剧本的 `pending` / `processing` 图片记录，并按普通分镜图、首帧、尾帧分别恢复本地运行状态、禁用对应按钮及重新接管任务轮询；现有链路完整，无需增加重复实现。
- 风险复现：修复前，Rehdasu 文本连接测试会调用 `/chat/completions`，图片连接测试会调用 `/images/generations`，存在“仅点测试连接也产生生成费用”的风险。
- 精准修复：仅对用户指定的 `rehdasu.cn` OpenAI 兼容服务改用只读 `GET /v1/models`，不提交提示词、不创建文字或图片生成任务。
- 能力边界：只读检测只证明网络和密钥可访问模型列表，不证明 `GPT-5.5` / `gpt-image-2` 已开通、额度充足或真实生成一定成功；配置页已有同义提示。
- 自动验证：新增 2 条回归测试，分别覆盖文本和图片配置，确认请求方法为 `GET`、无请求体；后端完整测试 55 条全部通过。本轮外部模型调用为 0。

### GPT Image 结果未知提示

- 风险：同步图片请求发生断线或网关中断时，供应商可能已经受理或扣费，但平台拿不到最终图片；原提示仅写“网络请求失败”，容易诱导收费用户立即重复提交。
- 修复：仅对 `gpt-image-*` 同步生成的网络异常提示增加“结果未知”“不要连续重试”“先核对生成记录或供应商账单”“避免重复扣费”，同时保留原始网络错误便于排查。
- 边界：Seedance 2.0 使用异步任务号轮询，仍按供应商任务状态恢复，不套用同步图片的结果未知提示。
- 配置页文案：文本连接成功也改为只声明 Key 与网络可用，不再误称文字生成已实际响应；明确模型名、权限与额度仍需真实创作任务验证。
- 自动验证：新增 1 条 GPT Image 风险提示回归测试；后端完整测试 56 条全部通过，前端生产构建成功。本轮外部模型调用为 0。

### 结果未知的人工重试门禁

- 行为：普通参数错误、权限错误等确定失败仍可直接重试；只有错误中明确包含“结果未知”时，重新生成首帧、尾帧或普通分镜图会弹出二次确认。
- 确认内容：明确告知供应商可能已受理或扣费，要求先核对生成记录或供应商账单；默认可取消，只有用户点击“仍要重新生成”才继续提交。
- 刷新边界：图片失败信息会持久化到 `storyboards.error_msg`，页面刷新后门禁仍然生效，不能通过刷新绕过。
- 成功清理：后续首帧或尾帧成功绑定时清除旧的分镜图片错误，避免已经成功的分镜继续携带“结果未知”并产生误提示。
- 自动验证：前端新增 3 条门禁测试，覆盖普通失败、确认继续、取消阻止，前端完整测试 7 条全部通过；后端新增首帧/尾帧成功清错 2 条测试，后端完整测试 58 条全部通过；生产构建成功。本轮外部模型调用为 0。

## 2026-07-13 Seedance 创建阶段扣费保护

- 风险边界：DJPSD 创建视频任务的 `POST` 若在供应商受理后发生网络断线，本地拿不到任务编号，原流程会记为普通失败并允许立即重试，存在重复创建付费任务的可能。
- 后端修复：仅将 DJPSD 创建请求的网络异常标记为“结果未知”，明确供应商可能已受理或扣费、要求核对供应商任务记录并避免连续重试；供应商明确返回的业务拒绝仍保持确定失败。
- 前端门禁：最新视频错误包含“结果未知”时，重新生成 Seedance 视频必须二次确认；取消则不会提交任何请求。
- 最新状态优先：分镜即使有旧的成功视频，较新的失败或结果未知记录也不再被旧成功记录掩盖；旧视频仍可播放，但最新错误会显示并参与重试门禁。最新一次成功后则不显示旧失败。
- 自动验证：DJPSD 新增 1 条创建断线风险提示测试，后端完整测试 59 条全部通过；前端新增 2 条最新状态选择测试，前端完整测试 9 条全部通过；生产构建成功。本轮外部模型调用为 0。
## 2026-07-13 Seedance 任务可追踪与轮询安全边界

- 目标：轮询到期不得误判失败并解除防重复锁；供应商任务编号可供用户和客服追踪；远程视频成功但本地保存失败必须明确提示。
- 后端：`provider_task_id`、时长、画幅和分辨率已返回前端；轮询到期返回“状态未知”，视频记录与本地任务继续保持 `processing`，因此同一分镜仍会命中防重复保护。
- 交付：远程视频成功而本地下载失败时仍保留可播放地址，同时写入“可在线播放但未保存本地”的警告，不引导重新生成。
- 前端：生成中显示供应商任务编号和“请勿重复提交”；已完成但未落盘时显示警告。
- 验证：后端 62/62、前端 11/11 测试通过；Vite 生产构建通过；`git diff --check` 通过。
- 费用边界：本轮未调用 GPT-5.5、gpt-image-2、Seedance 或其他付费生成接口。
## 2026-07-13 公开收费平台安全边界第一层

- 审计结论：现有应用仍是本地单机信任模型，未达到公网收费发布标准；详细问题记录在 `security_best_practices_report.md`。
- 管理边界：`PUBLIC_PLATFORM_MODE=true` 时，所有 `/api/v1/ai-configs` 接口必须使用独立的 `X-Platform-Admin-Token`；令牌缺失或短于 32 字符时默认拒绝。用户 JWT 仍独立使用 `Authorization` 请求头。
- 密钥边界：AI 配置列表、新建、详情和更新响应不再返回原始 `api_key`，仅返回 `has_api_key`；已保存配置的连接测试由后端使用 `config_id` 读取密钥，浏览器无需拿到密钥。
- 兼容性：本地模式默认保持原有访问方式；编辑既有配置时，密钥输入留空代表保留旧密钥。
- 验证：管理员中间件与密钥脱敏定向测试 5/5 通过；后端全量 67/67、前端 11/11、Vite 生产构建均通过。
- 费用边界：未调用 GPT-5.5、gpt-image-2、Seedance 或其他付费生成接口。

## 2026-07-13 用户身份与积分账本基础

- 目标：先建立可审计的用户身份和整数积分账本，暂不猜测模型售价，也不接入真实付费生成。
- 身份：新增邮箱注册、登录、当前用户接口；密码使用随机盐与 Node.js 内置 `scrypt` 保存，JWT 仅包含用户编号、邮箱和角色，有效期 2 小时。
- 公开模式：`PUBLIC_PLATFORM_MODE=true` 时，除注册和登录外的 `/api/v1` 业务接口必须携带用户 Bearer 令牌；`PLATFORM_JWT_SECRET` 少于 32 字节时默认拒绝。注册还必须显式设置 `PLATFORM_REGISTRATION_ENABLED=true`，否则保持关闭。
- 账户：注册成功创建零余额积分账户，不自动赠送额度。账本支持预扣、成功确认、明确失败退款，并以唯一操作号保证幂等；供应商结果未知时额度继续冻结，不自动确认或退款。
- 数据：迁移 23 新增积分账户、用量预留和不可重复事件账本；迁移 24 新增平台用户表。完整迁移在全新内存数据库执行成功。
- 原子性：JWT 密钥未安全配置时，在写入用户前直接拒绝，避免产生无法登录的半成品账户。
- 验证：身份、接口和账本定向测试 17/17 通过；后端全量测试 84/84 通过。
- 未完成边界：尚未设置 GPT-5.5、gpt-image-2、Seedance 2.0 的积分单价，生成接口也尚未绑定预扣记录；因此本步不部署为公开收费版本。
- 费用边界：本轮没有调用任何外部模型或付费生成接口。

## 2026-07-13 模型定价与图片计费闭环

- 定价边界：只允许 `GPT-5.5`、`gpt-image-2`、`seedance 2.0` 三个模型；单价使用正整数积分。系统不猜测默认价格，价格缺失时公开计费模式返回 `MODEL_PRICE_NOT_CONFIGURED` 并禁止创建生成记录。
- 原子预扣：图片异步任务、图片生成记录和积分预扣在同一数据库事务中创建；余额不足时三者全部回滚。相同分镜与帧类型的重复提交复用现有任务，不再重复预扣。
- 结算规则：图片本地确认成功后才将冻结积分确认为消费；供应商明确失败时退款；错误包含“结果未知”“状态未知”等不确定标记时继续冻结，等待人工核对，不允许系统误退款后再次生成。
- 数据关联：迁移 25 新增三模型积分价格；迁移 26 为图片和视频生成记录增加用户与积分预扣关联字段。视频计费接入仍未完成。
- 验证：定价测试 5/5、积分账本测试 9/9、图片计费测试 9/9、后端全量测试 101/101 通过。
- 发布边界：三模型价格仍为空，视频计费与管理端定价入口尚未完成，因此本轮不部署为公开收费版本。
- 费用边界：本轮外部模型调用为 0，未调用任何付费生成接口。

## 2026-07-13 登录、余额与管理员双令牌边界

- 用户入口：新增公开模式登录页；登录后请求自动携带用户 JWT，匿名用户访问业务页面会跳转登录并保留原地址。本地模式不强制登录。
- 积分可见性：新增 `GET /api/v1/billing/account`，只根据已认证的 `req.user.id` 返回当前用户的可用、冻结、已消费积分；前端右下角显示可用和冻结积分并支持退出。
- 管理入口：新增 `/billing-admin`，只管理三个指定模型的正整数积分价格；未定价状态明确显示为禁止生成。
- 身份冲突修复：用户身份继续使用 `Authorization: Bearer <JWT>`；管理员权限改用独立的 `X-Platform-Admin-Token`，解决同一请求无法同时通过用户和管理员验证的问题。
- 管理员令牌边界：只保存到浏览器当前会话，只随 `/ai-configs` 和 `/billing/prices` 请求发送，不附加到普通生成接口，也不写入构建产物。
- 部署文档：新增 `PUBLIC_PLATFORM_SETUP.md`，列出后端、前端环境变量、人工定价与小额验收门禁。
- 验证：管理员认证测试 4/4、计费接口测试 5/5、后端全量 116/116、前端全量 19/19 通过；Vite 生产构建通过。
- 未完成边界：支付充值、注册防滥用、请求限流、审计日志与生产小额真实验收仍未完成，因此不部署公开收费版本。
- 费用边界：本轮外部模型调用为 0，未调用任何付费生成接口。

## 2026-07-13 Seedance 视频计费闭环与管理端定价接口

- 创建原子性：Seedance 视频异步任务、视频生成记录和积分预扣在同一数据库事务中完成；缺价或余额不足时不留下任务、生成记录或预扣记录。
- 防重复扣费：同一分镜已有 `pending` 或 `processing` 视频时直接复用现有记录，不创建第二条预扣。
- 结算：视频成功落库后确认消费；供应商明确失败时退款；轮询最终状态未知、供应商任务仍可能处理中或创建结果未知时保持冻结，不自动退款，也不解除防重复保护。
- 接口状态：缺价返回 HTTP 503 与 `MODEL_PRICE_NOT_CONFIGURED`；余额不足返回 HTTP 402 与 `INSUFFICIENT_CREDITS`。
- 管理接口：新增 `GET /api/v1/billing/prices` 和 `PUT /api/v1/billing/prices/:model`。公开模式下先要求用户登录，再要求平台管理员令牌；只允许三个指定模型及正整数积分价格。
- 验证：视频计费定向测试 10/10、原有视频防重复测试 4/4、管理端定价接口 3/3、后端全量测试 114/114、前端测试 11/11 通过；全新内存数据库迁移成功，Vite 生产构建通过，`git diff --check` 通过。
- 发布边界：价格仍未由平台负责人确认和填写，前端登录、余额展示、充值与管理员定价界面尚未完成，因此仍不部署公开收费版本。
- 费用边界：本轮外部模型调用为 0，未调用任何付费生成接口。

## 2026-07-13 公开收费平台：限流与本人审计

- 新增本人审计接口 `GET /api/v1/billing/audit-events`；只按当前 JWT 用户编号查询，并限制返回条数。事件仅包含用户、资源、类型、结果和安全代码，不记录提示词、供应商密钥、邮箱、密码或原始 IP。
- 公开模式下，注册和登录为每来源哈希 15 分钟 10 次；模型生成白名单为每登录用户每分钟 20 次。守卫安装在用户认证之后，图片和视频原有的单独限流已移除，因此同一请求只记一次。
- 白名单包含 GPT 剧本/角色、图片、视频以及分镜提示词等高成本生成入口；普通更新和不支持的方法不会误计数。
- 自动验证：本人审计、生成守卫和限流定向测试 12/12 通过；后端全量测试 127/127 通过，且语法检查通过。
- 发布边界：这是一层防滥用措施，不证明所有历史生成入口都已经计费。图片和视频的规范路径已有原子预扣/结算，但公开收费发布前仍必须逐项确认每个可产生供应商费用的旧入口都接入账本，或在公开模式禁用。
- 费用边界：本轮外部模型调用为 0，未调用 GPT-5.5、gpt-image-2、Seedance 2.0 或其他付费接口。

### 最终补充验证

- 主图片与主视频生成路由注册新增回归测试，先复现误删后恢复；最终路由测试 1/1、本人审计/守卫/限流定向测试 13/13、后端全量测试 128/128 通过，三处新增或修改的后端文件语法检查通过。
