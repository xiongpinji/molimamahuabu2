# 全局账户积分区一致性任务

## 目标

将“可用积分 / 冻结积分 / 个人中心”作为同一个全局账户积分区，在所有已登录用户页面保持一致的数据来源、视觉样式和顶部展示位置。

## 页面范围

- 首页、短剧工厂、画布项目列表。
- 临时画布、独立画布、剧集画布。
- 剧集详情、短剧制作、素材库、自由创作、工作区与积分等已登录用户页面。
- 登录页不展示；个人中心独立页和弹层打开期间继续隐藏全局账户积分区。

## 根因

- `AccountBadge.vue` 已由 `App.vue` 全局挂载，并统一读取 `/billing/account`，数据链没有按页面分叉。
- 组件基础样式使用 `bottom: 18px`，只有画布路由通过 `account-badge--canvas` 改为 `top: 12px`，因此跨页面切换时位置不一致。
- 通用 `PlatformHeader.vue` 还包含一套账户下拉入口；若仅把全局账户积分区移到顶部，会与页头操作重叠。

## 交互约定

- 桌面端统一位于页面顶部操作栏：`top: 12px`。
- 通用页头页面贴近右侧；画布页面保留操作按钮所需的右侧空间，但垂直位置与样式一致。
- 公网平台模式由全局账户积分区承担个人中心入口；通用页头不再重复显示账户下拉，并为积分区预留空间。
- 移动端统一位于主导航下方，保持横向可滚动，不遮挡导航。
- 积分刷新继续复用 `moli:credit-account-refresh`，不增加第二套状态或请求。

## 验收标准

1. 登录后访问首页、短剧工厂、独立画布，账户积分区都处于同一顶部高度。
2. 三个页面展示同一套“可用积分 / 冻结积分 / 个人中心”结构和账户数据。
3. 通用页头上的兑换、主题、返回等操作不与账户积分区重叠。
4. 个人中心弹层打开、关闭和积分刷新行为不回归。
5. 单元测试、跨页面浏览器回归、生产构建和线上页面验证全部通过。

## 本地验证记录

- 账户、权限和画布位置相关单测：19/19 通过。
- 个人中心浏览器回归：7/7 通过，覆盖首页、短剧工厂、独立画布的账户积分区位置与数据。
- 生产构建：通过。
- 全量前端单测：471/477 通过；6 个失败都位于未修改的画布交互源码断言，相关源码和测试与 `origin/main` 一致，不属于本任务回归。
