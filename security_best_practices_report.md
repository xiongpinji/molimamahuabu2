# 茉莉妈妈公开收费平台安全审计（第一轮）

## 执行摘要

当前代码仍采用本地单机信任模型，不能直接公开收费运营。最优先风险是：所有业务 API 均无用户鉴权，AI 配置接口还能向请求方返回供应商密钥。修复必须先建立服务端身份和权限边界，再接入额度扣减；仅在前端隐藏入口不构成安全控制。

## 严重问题

### SEC-001：业务与付费生成接口无服务端鉴权

- 严重度：Critical
- 位置：`backend-node/src/app.js:65`；`backend-node/src/routes/index.js` 的全部 `/api/v1` 路由。
- 证据：应用直接挂载 `setupRouter`，路由中没有认证或用户身份中间件。
- 影响：任何能访问服务的人都能读取、修改、删除项目，并触发由平台承担费用的模型任务。
- 修复：公开模式必须默认拒绝匿名请求；增加用户身份、资源归属校验和管理员角色。额度扣减只能在服务端事务中执行。
- 缓解：在完整用户体系上线前，不得把服务端口直接暴露到公网。

### SEC-002：供应商 API 密钥可被匿名读取和修改

- 严重度：Critical
- 位置：`backend-node/src/routes/aiConfig.js:4-18,28-103`；`backend-node/src/services/aiConfigService.js` 的 `rowToConfig`。
- 证据：匿名请求可列出和修改配置；序列化结果包含原始 `api_key`。
- 影响：攻击者可窃取 GPT-5.5、gpt-image-2、Seedance 供应商凭据，直接产生平台外费用或破坏模型配置。
- 修复：配置管理接口强制管理员认证；响应永不回传原始密钥，只返回“已配置”状态；空密钥更新必须保留旧值。
- 缓解：立即限制 5679 端口只允许本机访问，并轮换任何曾对不可信网络开放过的密钥。

## 高风险问题

### SEC-003：没有服务端额度账本和原子扣减

- 严重度：High
- 位置：图片、视频及文本生成路由与服务。
- 证据：现有重复任务保护只能避免同一分镜的并发提交，没有用户余额、预占、确认、退款或幂等费用流水。
- 影响：无法可靠收费；并发、网络状态未知和任务失败可能导致超额消费或账实不符。
- 修复：建立不可变费用流水；提交前事务预占额度，供应商明确失败后退款，结果未知继续冻结，成功后确认扣减。

### SEC-004：生产安全响应头与请求滥用防护不足

- 严重度：High
- 位置：`backend-node/src/app.js:27-42`。
- 证据：未见 Helmet/等价响应头、接口限流；CORS 未配置时回退为 `*`。
- 影响：增加点击劫持、内容嗅探和批量滥用风险；鉴权上线后，宽松跨域会扩大攻击面。
- 修复：加入安全响应头、严格 CORS 白名单、登录和生成接口分级限流，并关闭 `X-Powered-By`。

## 中风险问题

### SEC-005：500 响应可能向客户端泄露内部错误

- 严重度：Medium
- 位置：`backend-node/src/app.js:103-109`。
- 证据：未处理异常时直接把 `err.message` 返回给客户端。
- 影响：可能暴露本地路径、数据库结构或上游响应细节。
- 修复：生产环境返回稳定错误码和通用文案，详细错误仅写入脱敏日志。

## 发布结论

当前结论：**禁止公网收费发布**。本地开发验证可继续。发布门禁至少要求 SEC-001、SEC-002、SEC-003 完成并通过权限隔离、并发扣减、失败退款和结果未知冻结测试。
