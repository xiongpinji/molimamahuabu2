# 画布视频节点首尾帧不可用修复记录

## 用户问题

画布模式下，视频节点编辑框中的“首尾帧”无法正常选择和使用。

## 根因

“首尾帧/多图参考”状态完全由当前连线的输入类型临时推断，没有保存到视频节点。没有参考图时点击“首尾帧”不会修改任何数据；之后新增的图片连线仍使用普通参考图输入，第二张图无法进入 `last_frame_url`。

## 修改范围

- 保存视频节点的参考模式。
- 首尾帧模式下，将前两张参考图依次映射为首帧和尾帧，后续参考图保持普通参考图。
- 新增或上传参考图后继续应用当前首尾帧模式。
- 不修改视频生成接口、模型选择、计费、其他节点或画布交互。

## 成功标准

1. 没有参考图时点击“首尾帧”，选择状态仍能保存并在刷新后恢复。
2. 第一张参考图写入 `first-frame`，第二张写入 `last-frame`。
3. 切换“多图参考”后，所有参考图恢复为 `reference-image`。
4. 后续新增或上传图片时自动采用当前模式。
5. 定向测试、画布相关测试和前端生产构建通过。

## 验证记录

- 修复前失败测试确认：模式字段被标准化过程丢弃；空参考图时模式函数不存在；前两张参考图无法映射为首帧和尾帧。
- 修复后生成数据定向测试 `19/19` 通过。
- 编辑框接线专项测试 `1/1` 通过。
- 首次浏览器回归发现恢复已有参考边时未触发首次槽位校正；增加首次挂载校正后，同一路径通过。
- 首尾帧相关浏览器回归 `2/2` 通过，覆盖空参考图选择、持久化、刷新恢复、两张参考图首尾槽位和切回多图参考。
- 前端全量 Node 测试 `566/576` 通过；10 个失败与同一 `origin/main@3908861` 基线逐项一致，均为未修改的旧画布静态契约。
- 前端生产构建通过，共转换 `1843` 个模块。
