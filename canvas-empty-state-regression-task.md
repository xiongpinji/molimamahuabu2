# 画布空状态误显示修复

## 问题

独立画布已经加载真实节点时，页面仍渲染“暂无画布数据”空状态。该元素被排在占满容器的 VueFlow 后面，形成工具栏下方额外的大块空白区域。

## 根因

空状态的 `v-else-if` 实际与紧邻的水平对齐辅助线 `v-if` 配对，而不是与前面的 VueFlow 条件配对。未显示辅助线且加载结束时，空状态因此会在已有节点的画布中继续渲染。

## 成功标准

- 独立画布存在节点时，不渲染“暂无画布数据”。
- 独立画布仍保持完整可交互的 VueFlow 画布区域。
- 非独立画布在未加载且没有节点时，继续保留原有空状态。
- 新增浏览器回归断言，修复前失败、修复后通过。
- 前端相关测试、生产构建、PR CI 和线上浏览器复核全部通过。

## 修改边界

- 只修改 `DramaCanvas.vue` 的空状态渲染条件和对应回归测试。
- 不调整画布节点、工具栏、数据结构或相邻样式。

## 验证记录

- 线上 `/canvas/4` 连续两次复现：VueFlow 节点数 `22`，同时存在“暂无画布数据”。
- 新增 Playwright 断言在修复前失败：节点可见，但空状态数量为 `1`。
- 修复后使用独立开发端口复测：专项 Playwright 用例 `1/1` 通过。
- 画布设置相关单元测试 `7/7` 通过。
- 画布持久化脚本通过。
- 生产构建通过，共转换 `1807` 个模块。
- 完整独立画布套件 `6/8` 通过；另外两个失败在未修改的 `origin/main@d25f8ae` 上以相同报错复现，确认为既有基线问题，不纳入本次修改范围。
- 前端全量 Node 测试 `451/456` 通过；其余五个静态契约失败也与未修改的 `origin/main@d25f8ae` 完全一致。
- Standards 与 Spec 双轴审计均无发现。
