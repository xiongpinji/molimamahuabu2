# NeoDomain 画布设置升级任务

## 目标与边界

- 升级完整业务画布 `DramaCanvas.vue`（项目画布 `/canvas/:id`），不修改首页轻量画布。
- 以已登录的 NeoDomain 可见界面为交互和视觉参照，不读取、复制或依赖其私有源码。
- 设置必须作用于真实画布行为并随项目画布布局保存；禁止只做外观、控件不生效。
- 保持现有节点、连线、生成队列、撤销重做和布局保存能力不回退。

## 参照结构

- 底部工具栏齿轮打开宽 `420px`、高 `400px` 的“自定义画布”浮层。
- 顶部包含“恢复默认”、停靠按钮和关闭按钮。
- 左侧目录定位右侧连续滚动区域，共 8 类：
  1. 交互操作
  2. 连线设置
  3. 网格与显示
  4. 节点与布局
  5. 自定义背景
  6. 画布主题
  7. 连线色彩
  8. 简化配色

## 设置清单与默认值

### 交互操作

- 画布平移灵敏度：`0.2–5`，步长 `0.1`，默认 `1.5x`
- 空白区域操作：默认“右键菜单”，可选“双击菜单”
- 图/视延迟提交：`0–10s`，默认关闭
- 滚轮默认操作：默认“平移”，可选“缩放”
- 触摸点吸附范围：`50–300px`，默认 `150px`
- 拖拽吸附网格：默认开启
- 对齐辅助线：默认开启
- 节点提交后取消焦点：默认关闭
- 极简模式缩放：默认开启
- 联动预览画布素材：默认开启

### 连线设置

- 连线显示粗细：`1–6px`，默认 `2px`
- 连线焦点范围：`6–40px`，默认 `12px`
- 开启连线动画：默认关闭
- 仅显示焦点连线：默认关闭

### 网格与显示

- 网格线间距：`6–60px`，默认 `20px`
- 网格点大小：`1–6px`，默认 `2px`
- 显示网格底纹：默认开启
- 背景氛围光晕：默认关闭
- 显示导航小地图：默认关闭

### 节点与布局

- 整理节点水平间距：`50–200px`，默认 `100px`
- 整理节点垂直间距：`80–300px`，默认 `110px`
- 打组包含边距：`50–120px`，默认 `80px`
- 上方工具栏缩放：`0.8–2x`，默认 `1x`
- 下方工具栏缩放：`0.8–2x`，默认 `1x`

### 自定义背景

- 启用背景图：默认关闭
- 背景图片：从当前画布真实图片素材中选择
- 不透明度：`5–100%`，默认 `25%`
- 模糊度：`0–40px`，默认 `8px`
- 显示方式：默认“铺满”，可选“适应”或“平铺”
- 平铺大小：`10–300%`，默认 `100%`

### 色彩

- 画布主题、连线色彩和简化配色均提供 NeoDomain 可见设置中的同名预设。
- 默认画布主题为“玄黑”，默认连线色彩为“默认”，默认简化配色为“跟随主题”。
- 自定义画布主题和连线色彩允许使用浏览器原生颜色选择器。

## 验收标准

- [x] 浮层尺寸、层级、左侧目录、右侧滚动、激活态、滑杆、开关和配色选项与参照一致。
- [x] “恢复默认”、关闭、Esc 关闭和目录定位可用；小屏幕不溢出视口。
- [x] 每个设置均接入对应真实行为或样式，不存在无效开关。
- [x] 设置写入并从 `canvas_layout.preferences` 恢复，旧项目数据可安全归一化。
- [x] 自动整理间距、打组边距、工具栏缩放、网格、小地图、吸附、滚轮模式、背景和连线样式均有回归验证。
- [x] 本地单元测试、静态契约测试、前端生产构建通过。
- [x] 在真实浏览器中逐类验证设置、刷新持久化、恢复默认和关键画布操作。
- [ ] 更新 PR，等待 CI 全绿；合并后执行生产备份、canary 验证、部署和线上健康检查。
