# 个人中心线上真实数据装载修复

## 现象

2026-07-30 部署后使用既有登录账户验收时，顶部账户入口显示真实余额 926，但个人中心主体显示余额、工作区和作品均为默认值。

## 根因假设与成功标准

个人中心使用 `Promise.all` 并行请求六组数据，任一非核心接口失败会跳过全部赋值。修复必须满足：

- 单个接口失败时，其他已成功接口仍写入真实数据。
- 不用假数据掩盖失败。
- 既有完整成功链行为保持不变。
- 本地行为测试、定向 E2E、生产构建、PR CI 与线上真实登录复验全部通过。

## 边界

- 只修改个人中心数据装载与对应测试。
- 不修改账户、积分、作品和租户接口语义。
- 不修改画布、图片编辑和生成任务。

## 本地验证记录

- 相关行为测试：11/11 通过。
- 个人中心 Playwright E2E：4/4 通过；其中故意让审计接口返回 500，余额、工作区和作品仍加载成功；积分接口返回 500 时明确显示加载失败，不伪装成零余额。
- 前端生产构建：通过，产出 `personal-center` 独立资源。
- 差异检查：`git diff --check` 通过。
