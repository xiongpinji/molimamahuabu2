# 画布连线设置无效修复记录

## 用户问题

线上独立画布 `/canvas/4` 的“自定义画布 → 连线设置”中，调整连线粗细后现有连线没有视觉变化；连线色彩也未覆盖现有业务连线颜色。

## 线上复现证据

- 画布存在 2 条现有连线。
- 设置面板的 `edge_width` 从 `5.8px` 更新为 `6px`。
- `.canvas-main` 上的 `--canvas-edge-width` 同步更新为 `6px`。
- 实际 `.vue-flow__edge-path` 仍为内联 `stroke-width: 1.8`，计算值仍为 `1.8px`。
- 连线色彩变量已为玫红色，但实际路径仍为内联青色。
- 连线焦点范围、动画开关和“仅显示焦点连线”已确认能够改变实际渲染状态。

## 根因

业务连线由 `CanvasCuttableEdge` 渲染，边数据携带固定的内联 `stroke`、`stroke-width` 和 `stroke-dasharray`。`DramaCanvas.vue` 虽然生成了响应式画布设置变量，但普通 CSS 无法覆盖 SVG 路径的内联样式。

## 修复范围

- 让画布设置的连线基础色、焦点色、粗细和动画虚线优先于业务连线内联样式。
- 保留连线类型、剪断、运行下游节点和业务数据。
- 不修改后端、数据库结构、节点数据和生成链路。

## 成功标准

1. 现有连线的计算粗细随 `edge_width` 实时变化。
2. 现有连线颜色随连线色彩设置实时变化。
3. 动画打开时使用统一的动画虚线样式，关闭后保留原有业务线型。
4. 选中/悬停焦点色、“仅显示焦点连线”和焦点范围继续生效。
5. 设置刷新后仍由现有画布布局持久化链路恢复。
6. 定向测试、前端构建、CI 和线上真实浏览器复验全部通过。

## 本地验证记录

- 修复前新增回归用例失败：设置值为 `6px`，现有连线计算值仍为 `1.8px`。
- 修复后同一回归用例通过，已覆盖现有内联连线的粗细、颜色、焦点色、焦点范围、仅焦点显示、动画开关及刷新持久化。
- 审计后将强制样式限定到 `CanvasCuttableEdge`，不覆盖已自行读取偏好设置的 `LibTvCanvasEdge`。
- 连线设置定向 E2E：`1/1` 通过。
- 画布设置与底部工具栏静态测试：`7/7` 通过。
- 画布持久化脚本：通过。
- 前端生产构建通过：`npm run build`。
- 当前主分支对照测试确认：
  - 静态画布测试为 `36/39` 通过，3 个旧断言在原始主分支同样失败。
  - 独立画布 E2E 为 `6/8` 通过，右键复制入口和多结果入库断言在原始主分支同样失败。
- 上述 5 个既有失败均不涉及本次修改文件和连线设置链路，不作为本次回归。
