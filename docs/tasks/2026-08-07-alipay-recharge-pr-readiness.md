# 支付宝自主充值 PR 前验收记录

日期：2026-08-07
分支：`codex/alipay-self-recharge`

## 本次目标

- 将充值分支同步到最新 `origin/main`，不推送、不部署。
- 复核用户固定比例充值、管理员套餐、广告展示、异步通知入账和租户隔离。
- 在创建 PR 前完成安全、回归、构建、依赖和浏览器验收。

## 本次收口

- 支付网关只允许：
  - `https://openapi.alipay.com/gateway.do`
  - `https://openapi-sandbox.dl.alipaydev.com/gateway.do`
- 后端同时校验环境变量网关和 SDK 生成的支付地址；前端再次校验跳转地址。
- 新增账务流水失败时整笔事务回滚测试，覆盖订单状态、交易号、余额和流水。
- 新增跨租户订单查询、通知入账隔离测试。
- 新增管理员套餐 list/create/update/reorder 的真实 HTTP 权限矩阵测试。
- 将前端锁文件中的 `postcss` 从 8.5.18 升级到 8.5.26，消除依赖审计中风险项。

## TDD 证据

- 临时移除入账事务时，新增回滚测试失败；恢复事务后通过。
- 临时移除管理员列表权限门禁时，新增 HTTP 权限测试失败；恢复门禁后通过。
- 网关白名单实现前，恶意 HTTPS 域名与 SDK 非官方地址测试失败；实现后全部通过。

## 最终验证

- 后端充值定向测试：63 项，62 通过，1 项因当前 Windows 环境无法创建 symlink 而跳过，0 失败。
- 前端充值定向测试：40/40 通过。
- 后端完整测试：713 项，712 通过，1 项同上跳过，0 失败。
- 前端完整测试：623/623 通过。
- 前端生产构建：通过；只有既有大分块警告。
- 后端生产依赖审计：0 漏洞。
- 前端生产依赖审计：0 漏洞。
- 充值中心 Playwright：14/14 通过，覆盖支付暂停、失败恢复、三种视口、230px 广告图、管理员全字段编辑、上传、排序和并发互斥。
- `git diff --check`：通过。

## 证据边界与下一门禁

- Playwright 使用本地服务和受控接口响应，不代表支付宝真实支付成功。
- 本次未写入企业支付宝密钥，未发起真实扣款，未推送分支，未创建 PR，未部署生产。
- PR 合并后仍须按预发布流程配置密钥和公网回调，完成一笔受控低金额支付，并核对验签、订单、余额、账本、重复通知幂等和审计记录；在该门禁通过前不得开放生产充值。
