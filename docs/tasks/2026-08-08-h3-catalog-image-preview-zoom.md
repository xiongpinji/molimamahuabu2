# H3 目录与图片预览缩放修复记录

## 目标

1. 查明 MiniMax H3 从前端统一模型目录消失的原因。
2. 图片节点双击全屏预览后，支持 `Ctrl/Command + 鼠标滚轮` 缩放。

## 根因与处理

- H3：生产 `ai_service_configs` 仍为启用且已验证，但 `model_credit_prices.model = minimax h3` 的状态为 `disabled`，统一目录按门禁正确隐藏。2026-08-08 使用现有 Key、480P、4 秒、非人物参考图进行一次真实生成，供应商任务 `task_elIwVklACOvOZwk5diClsGq1sxEETNpq` 在 13.9 秒后明确失败，原因为“服务器暂时无可用资源，请稍后重试”。未取得可读成片前不得恢复为 `enabled`。
- 图片预览：全屏层只有静态图片，没有缩放状态和滚轮处理；全局页面缩放保护又会阻止浏览器默认缩放。新增独立预览缩放状态，只有 `Ctrl/Command + 滚轮` 时按 15% 步进缩放，范围 25%–500%，打开和关闭预览均复位到 100%。

## 验收标准

- 双击图片打开全屏预览后，修饰键加滚轮能够实际改变图片比例并显示百分比。
- 普通滚轮和视频预览不触发图片缩放。
- 关闭再打开时回到 100%。
- H3 继续保持门禁隐藏，直到同一 Key 的真实生成成功、成片可读取并完成证据记录。

## 自动验证

- TDD 红灯：新增缩放合同后，因缺少 `mediaPreviewScale` 正确失败。
- 定向回归：63/63 通过。
- 生产前端构建：1855 modules，成功。

## 飞拓替代模型接入进度

- 已接入精确上游 ID：`xuan-video-v1-6e7b4763634e6206`（MiniMax H3-2K）和 `xuan-seedance-2.5`（仅 480P/720P）；未接入裸 `seedance-2.5`。
- 2026-08-08 真实请求确认 `xuan-video-v1-6e7b4763634e6206` 仅支持 15 秒；首次 5 秒请求被供应商以 HTTP 400 明确拒绝，未自动重试，随后将全站能力与验证矩阵收紧为仅 15 秒。
- 两层生成门禁已落地：创建任务/预扣前校验一次，供应商 POST 前再次校验；未真实验证、缺能力或缺价格时副作用为 0。
- 管理员预设、首页、画布和短剧工厂统一使用 `/canvas/model-catalog`，展示名与公开备注由管理员价格配置同步。
- 已完成可恢复的三组付费验证脚本：H3 2K/5 秒、Seedance 480P/4 秒、Seedance 720P/4 秒；记录创建、生成、下载和总耗时。创建连接中断或明确失败后禁止自动重试。
- 已完成单事务配置工具：复用现有飞拓凭据但不输出密钥；H3 成本 1.50 元/次、扣 1313 积分/次；Seedance 成本 0.40 元/秒、扣 350 积分/秒；支持计划、应用、验证、受保护回滚。
- 当前新模型仍保持隐藏。只有三组真实生成、MP4 读取、`ffprobe`、SHA-256 和公网读取全部成功后，配置事务才允许开放。

### 本地证据

- 后端整组回归：99/99 通过。
- 飞拓验证/配置/公开成品路由定向回归：21/21 通过；配置事务 3/3 通过。
- 前端模型合同与画布相关回归：41/41 通过。
- 生产前端构建：1855 modules，成功。
