# 供应商容灾保护阶段

## 目标

在不调用付费供应商、不部署代码、不重启服务的前提下，清理已停用 USMercari 路由遗留的三条孤立价格，并以实时线上 release 的精确副本验证文本、图片供应商切换、幂等和积分结算保护。

## 精确范围

1. 仅将 `model_credit_prices` 中以下三条仍为 `enabled` 的价格改为 `disabled`：
   - `minimax h3`
   - `gpt-image-2-2-4k`
   - `nano-banana-2`
2. 不修改其余七条孤立价格，不修改其他模型配置、密钥或价格。
3. 从实时 `/opt/moli-drama/current` 复制隔离测试副本；不得用本地旧代码冒充线上版本。
4. 只执行本地、无供应商网络请求的确定性故障注入；不得发起真实生成。
5. 如发现代码缺口，只在隔离工作树内按 TDD 修复并审查；本阶段不推送、不发布、不切换 production。

## 成功标准

1. 三条价格更新前有可读数据库备份，备份与线上数据库均为 `quick_check = ok`。
2. 更新在部署锁和实时 release CAS 下执行，且只精确影响三条目标价格。
3. 操作前后账户积分、图片/视频生成记录数、活动任务数不变；服务与 AI 音乐进程不重启。
4. 隔离副本中的故障注入证明：
   - 主供应商明确未受理，备用供应商成功时，只产生一次积分预留与一次最终结算；
   - 结果未知时不自动切换、不重复提交，并保持需要人工确认的积分状态；
   - 相同幂等键不会创建重复路由请求；
   - 管理员事件可识别供应商配置和安全分类，且不泄露密钥。
5. 若现有实现不能满足上述合同，先形成可复现红灯，再做最小修复并通过定向与回归测试。

## 执行门禁

- 真实供应商生成、付费调用、线上代码部署、服务重启和数据库范围扩张均需另行明确批准。
- 未获得真实生成授权前，不得把确定性测试宣称为真实上游切换验收。

## 状态

PR #163 已合入 `main`，受保护发布已激活到
`/opt/moli-drama/releases/provider-failover-pr163-20260816-1f8f2e93`。

后续浏览器验收锁阶段只新增管理员稳定性面板的 Playwright 回归，不修改业务代码；
未调用付费供应商，未修改线上配置、数据库或服务，未推送或部署该测试提交。

## 生产价格清理证据

- 实时 release：`/opt/moli-drama/releases/usmercari-static-pr162-20260816-c4612d20`。
- 在部署锁和实时 release CAS 下，仅将以下三条价格由 `enabled` 改为 `disabled`：
  - `gpt-image-2-2-4k`：70 积分；
  - `minimax h3`：950 积分；
  - `nano-banana-2`：70 积分。
- 备份：`/opt/moli-drama/shared/backups/disable-usmercari-3prices-20260816T040510Z.sqlite`。
- 备份 SHA-256：`b1cb34b7521c454087d90a1cb3aff4b9305e9d66392fef0137999db569e395cd`。
- 备份与线上数据库 `quick_check = ok`；其余价格和模型配置未修改。
- 操作前后账户均为：可用 63876、冻结 240、已消费 48084；图片 561、视频 308、活动任务 0。
- `moli-drama` 未重启，本地与公网健康检查均为 200；AI 音乐 PID 206874、206895 保持运行。

## 精确线上副本证据

- 隔离副本：`C:\Users\canqu\AppData\Local\Temp\codex-provider-failover-live-c4612d20-source`。
- `src`、`test`、`migrations` 共 471 个文件，`configs` 1 个文件，与实时 release 清单逐文件一致，差异 0。
- `package-lock.json` SHA-256：`d58850ec44893f4b8206acb2f4aac1294e41087b4ed679331db4d1a20158680e`，与实时 release 一致。
- `package.json` SHA-256：`235dc215cd97d883b203b96a90a580cb0225aa810cbbf862e3de9cf5b4b523e0`，与实时 release 一致。
- 精确线上源码配合当前安全合同测试：文本、图片、路由、管理员、对账共 44/44 通过；未访问真实供应商。
- 旧图片合同中的两个失败已确认为测试输入过时：一项未声明参考图能力，另一项把普通文本伪装为图片 Base64；改用明确能力声明和有效 PNG 字节后，图片路由合同 13/13 通过，未削弱线上输入保护。

## 本地缺口与修复

1. 实际切换供应商时只写普通日志，没有写管理员 `route_switched` 事件，导致 `last_switch_at` 长期为空。
2. 熔断冷却后的 `claimHalfOpen` 没有接入真实 attempt 创建路径，多请求可同时探测同一个半开供应商。
3. 已按 TDD 最小修复：
   - 文本、图片、视频真正开始备用 attempt 时写安全的 `route_switched` 事件；
   - `startAttempt` 在同一 SQLite 事务中完成半开 CAS，只允许一个探测；
   - 被其他请求占用的半开供应商不会重复提交、不会误记切换，路由写入明确失败终态；
   - 半开探测基础设施失败后重新进入冷却并释放 claim，冷却结束可再次认领；策略/参数类非健康终态会恢复健康并释放 claim；
   - 管理员页面继续只显示中转站主机名和安全分类，不暴露密钥、请求正文或签名 URL。

## 验证结果

- 红灯：切换事件缺失时，文本、图片、视频 3 项断言稳定失败；半开 CAS 缺失时，第 2 个 attempt 被错误创建；客户端未处理空 attempt 时复现 `attempt_no` 空引用。
- 相关路由、计费、对账测试：93/93 通过。
- 后端全量：866 项，865 通过、1 项按设计跳过、0 失败。
- 9 个改动源码/测试文件 `node --check` 全部通过；`git diff --check` 通过。
- 功能锁门禁（以 `HEAD` 为基线）通过：4 个锁定功能、10 个变更路径均有本轮批准的解锁与影响测试记录。
- 独立最终复审：APPROVE，0 个阻断；确认原半开 claim HIGH 问题已关闭，未发现新的状态、幂等、计费或误记切换问题。
- 未调用付费供应商，未修改生产代码，未重启服务；代码已形成独立提交，尚未合并或部署。

## 发布后浏览器验收锁（2026-08-16）

- 本地工作树与最新 `origin/main` 文件树一致；线上 `current` 仍指向 PR #163 对应 release。
  本阶段在独立分支 `codex/provider-failover-admin-browser-lock-20260816` 上执行。
- 后端本地真实 HTTP 故障注入 38/38 通过：主上游返回结构化 503，备用上游成功，
  路由尝试为 2 次，写入 `route_switched`，最终绑定备用配置，积分只预留和结算一次；
  普通用户结果不包含供应商、中转站、上游模型或原始错误。
- 管理员 API/前端安全契约 7/7 通过；新增 Playwright 浏览器验收 2/2 通过：
  管理员可见故障、自动切换、中转域名和积分状态，普通用户无法进入稳定性管理页，
  页面不显示密钥、签名 URL 或提示词全文。
- 前端全量单元/契约测试 659/659 通过；生产构建通过。
- 线上只读复核：`moli-drama` 为 `active`，PID 2129833、`NRestarts=0`，
  内外健康检查均为 200，最近 15 分钟错误级日志为 0；数据库 `quick_check=ok`，
  图片、视频和异步活动任务均为 0，10 条路由请求均为 `succeeded`；部署锁无持有者。
- AI 音乐保持隔离：`/opt/moli-mama` 的 server/worker PID 仍为 206874/206895。
