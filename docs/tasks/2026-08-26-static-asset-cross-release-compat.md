# 旧标签页跨版本静态资源兼容修复

## 问题

生产发布后，已打开的旧标签页仍运行旧入口脚本；当它继续加载旧版动态分块时，新 release 已删除对应哈希文件。后端随后把缺失的 `/assets/*.js` 回退为 `index.html`（HTTP 200、`text/html`），浏览器因此报动态模块加载失败。

## 本阶段边界

- 只修改隔离工作树中的前端构建、前端恢复入口和后端静态资源服务。
- 已获授权推送并完成 PR 合入流程；仍不部署、不修改生产数据库、不触发供应商或付费调用、不触碰 AI 音乐。
- 不改变 `/api`、`/static` 用户素材、画布业务逻辑或短剧工厂业务逻辑。

## 成功标准

1. 已存在的 `/assets/*` 正常返回，缺失的 `/assets/*` 返回真实 404，绝不返回 SPA HTML。
2. 每次前端构建保留本次资源与紧邻上一版本的资源，并清理更老兼容资源，避免无限增长。
3. 新版入口捕获 Vite 动态分块加载失败时至多自动刷新一次；短时间再次失败时不循环刷新。
4. SPA 路由仍返回 `index.html`，HTML 明确要求重新验证缓存。
5. 聚焦测试、前端生产构建和相关既有回归全部通过。

## 非目标

- 不引入 Service Worker。
- 不取消 Vite 代码分割或改成无哈希文件名。
- 不扫描其他 release 目录，也不让运行时读取生产发布历史。

## 实现边界

- 后端仅调整 `frontweb/dist` 的静态资源挂载：`/assets` 缺失时返回非 HTML 404，真实 SPA 路由继续回退到 `index.html`。
- 前端构建清单只保留当前代和紧邻上一代；候选被重复构建时仍保留原线上上一代，不把构建次数误判为发布代数。
- 客户端只在 Vite 动态分块预加载失败时刷新一次，并用 30 秒会话冷却阻止刷新循环。
- 精确发布白名单包含 11 个功能文件与 2 个功能锁治理文件；不包含 `frontweb/dist`、生产数据库、上传目录、模型配置、积分逻辑或 AI 音乐。

## 本地验证证据

- 基线：分支 `codex/static-asset-cross-release-compat-20260826` 最初基于 PR #193 合并提交 `3971787c5d50aac4865ea1d3da78c572e051fe24`，并已同步包含 urllib 安全修复 PR #196 的最新 `origin/main` 合并提交 `57013830a3b53d2af416975e452f7815095a364d`。
- 后端聚焦回归：42 项通过、0 失败，覆盖静态资源 404/SPA、生产部署合同、增量范围、依赖安全与后台服务合同。
- 前端聚焦回归：35 项通过、0 失败，覆盖两代资源保留、重复构建、预加载单次恢复、登录、画布副本连线及结果未知停止重提。
- 前端全量单测：936 项中 935 项通过；唯一失败为 `canvasInteractionEntrypoints.test.js` 对 `layoutSaveState.value === 'saving'` 的旧文本断言。该失败已在未包含本 PR 改动的最新 `main` 基线原样复现，且对应测试与 `DramaCanvas.vue` 均不在本 PR 差异内，因此不扩大本次静态资源兼容修复边界。
- 真实生产构建首次接管：总资源 123；当前代 83；上一代 40。
- 同一候选重复生产构建：仍为总资源 123；当前代 83；上一代 40；上一代缺失 0。
- PR #195 首轮 Hosted CI 暴露 `backend-node/src/app.js` 同时属于“未知状态积分对账”和“主动巡检公开证据”两个已锁定路径；已按产品负责人 2026-08-26 批准补充新鲜 unlock，并把 PR #193 当前批准顺序归档到 `unlockHistory`。
- `git diff --check` 与精确 13 文件发布范围对账必须在交付前再次通过。

## 尚未执行

- PR #195 已创建；同步主线后的功能锁治理改动纳入本提交。未部署、未切换 `/opt/moli-drama/current`。
- 未写生产数据库、未调用供应商、未产生付费、未触碰 AI 音乐。
- 生产验收必须在后续单独授权后，从操作时实时 `current` 构建受保护增量候选并执行共享门禁、CAS、健康与日志检查。
