# 画布连线落点闭环

## 目标

1. 从节点拖出的连线落到另一节点主体任意位置时，自动连接到该节点。
2. 连线落到画布空白处时，打开现有“添加节点”菜单。
3. 从该菜单创建自由节点后，自动把起始节点连接到新节点。

## 验收

- 无需命中节点两侧的“+”连接柄即可完成连接。
- 落到编辑器、工具栏等非画布区域时不误弹菜单。
- 正常右键菜单、原有连接柄连线与重复连线校验保持不变。
- 单元测试、前端构建与浏览器交互回归通过。
