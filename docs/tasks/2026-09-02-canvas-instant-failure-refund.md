# 画布失败积分秒退与存量冻结返还

## 目标

- 画布调用方明确标记为 `failed` 时，在同一结算调用内退回预扣积分。
- 明确标记为 `needs_attention` / `TEXT_RESULT_UNKNOWN` 的任务继续保持冻结，避免供应商仍可能成功时重复退款。
- 按用户授权，一次性返还操作时线上全部存量冻结积分。

## 代码变更

- 场景提示词和通用文本计费的明确失败使用结构化 `failureDisposition: refund`，不再由错误文案决定是否退款。
- 视频生成记录进入 `failed` 时默认使用结构化退款；调用方显式传入的其他结算方式仍优先。
- 回归测试覆盖错误文案含“结果未知”但调用方已明确失败时的退款，以及真正 `TEXT_RESULT_UNKNOWN` 继续冻结。

## TDD 证据

新增回归测试在修改实现前失败：

- 画布场景提示词：实际 `held`，预期 `refunded`。
- 画布视频：实际 `held`，预期 `refunded`。

修改后相关测试集共 `182/182` 通过。

## 生产存量返还记录

- 操作前实时版本：`/opt/moli-drama/releases/canvas-recharge-login-fix-20260902-d1c426ad-r1`。
- 操作前冻结：2 笔，共 20 积分；个人租户账户；均为 2026-07-31 的 `scene_single_image_prompt` 预扣，未找到关联异步任务、图片、视频或供应商路由证据。
- 操作前活动中的异步任务、图片任务和视频任务均为 0。
- 数据库备份：`/opt/moli-drama/shared/backups/drama_generator.db.pre-refund-20260902-20260902T074427Z.bak`。
- 备份 SHA-256：`f29a2d8afb50ba92bfb49abd57cf2883c834a3fc7806ed208326fc03b9618319`；`quick_check=ok`。
- 退款事务时间：`2026-09-02T07:46:25.456Z`。
- 事务断言：操作时冻结范围必须仍为 2 笔、20 积分，且活动任务必须为 0，否则整体回滚。
- 操作后：冻结 0 笔、0 积分；退款账本 2 条，可用积分增加 20，冻结积分减少 20；对账审计 2 条，安全码 `admin_authorized_bulk_refund_all_held`。
- 操作后数据库 `quick_check=ok`，服务为 `active`，内部健康检查正常。

## 发布状态

本任务的代码修改尚未部署。已从实时 `current` 制作候选 `canvas-instant-refund-20260902-5f1f5513-r2`，候选变更范围精确为上述 3 个运行时代码文件；候选回归测试 `155/155` 通过，Node 语法检查通过。

共享门禁 verify-only 已通过画布积分合同、画布引用合同、构建和模型目录检查，但因 ToAPIs 私有头像证据生成时间超过 24 小时而拒绝候选。未执行停服、数据库备份或 `current` 切换；r1（补丁换行不兼容）和 r2（全局证据过期）候选均保留供审计。后续生产发布必须先刷新并审查该外部模型证据，再重新执行 verify-only，最后才允许受保护激活。
