# 预发布可运营性验证报告（2026-07-24）

## 本阶段结论

生产预检、SQLite 一致性备份、备份校验和非覆盖恢复演练已经形成可重复执行的最小闭环。

当前开发环境仍不满足公网发布条件。本报告不把本地测试通过等同于可上线。

## 自动验证

| 验证项 | 结果 |
| --- | --- |
| 预检与备份目标测试 | 11/11 通过 |
| 后端全量测试 | 376/376 通过 |
| 前端全量测试 | 288/288 通过 |
| 前端生产构建 | 通过 |
| 新增 Node 脚本语法检查 | 通过 |
| 前端生产依赖审计 | 0 个漏洞 |

## 实际操作验证

已使用当前开发数据库执行一次独立临时目录演练：

1. 在线备份创建成功。
2. 备份 SHA-256 与 SQLite `quick_check` 通过。
3. 备份列表可读，清单包含时间、大小、哈希和完整性状态。
4. 恢复到一个不存在的新数据库文件成功。
5. 恢复文件哈希与备份清单一致。
6. 原数据库未被覆盖或替换。

## 当前预检结果

当前仓库开发配置的 `ready` 为 `false`，符合预期。主要阻塞项：

- 未设置公开平台生产环境变量。
- 调试模式仍启用。
- `server.insecure_tls` 仍启用。
- CORS 和素材地址仍指向 localhost/HTTP。
- `GPT-5.5`、`gpt-image-2`、`seedance 2.0` 尚未全部配置积分价格。

数据库 `quick_check` 和活跃管理员检查通过。

## 发布阻塞项

1. 后端官方 npm 审计存在 2 个高危公告：
   - `adm-zip < 0.6.0`
   - `sharp < 0.35.0`
2. 两项官方修复均要求破坏性版本升级，必须在独立阶段升级并执行 ZIP 导入、图片处理和全量回归。
3. 尚未使用正式供应商密钥与隔离额度执行真实小额生成及账单对账。
4. 正式域名、HTTPS、反向代理、日志脱敏、告警和异地备份尚未配置。

以上阻塞项清零前，不得宣称公网收费平台可上线。
