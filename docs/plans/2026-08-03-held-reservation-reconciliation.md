# 历史 held 预扣对账与防复发计划

## 范围

- 只处理茉莉妈妈后端的积分预扣证据链与防复发代码。
- 不修改线上积分、预扣状态、生成记录或项目内容。
- 不触发文本、图片、视频或合成生成。
- 不触碰 AI 音乐服务。
- 发布时只上传本次实际修改的后端文件。

## 线上只读结论

2026-08-03 读取线上数据库得到 15 条 `held` 租户预扣，共 500 积分，且没有运行中的异步、图片、视频或合成任务。

| 分类 | 数量 | 积分 | 处理原则 |
| --- | ---: | ---: | --- |
| 服务重启后明确失败的图片任务 | 4 | 240 | 现有证据允许退款，但必须先取得用户授权 |
| 供应商结果未知 | 5 | 200 | 保持 `held`，先核对供应商记录或账单 |
| 文本调用缺少本次终态证据 | 6 | 60 | 保持 `held`，后续重试成功不能替代原调用证据 |

## 已确认的防复发缺口

1. 图片预扣 ID 只写入 `image_generations`，没有同步写入同事务创建的 `async_tasks`。服务重启清理遗留任务时因此无法结算图片预扣。
2. 角色四视图、场景四视图和场景单图的文本提示词调用已经成功后，仍要等下游图片任务提交成功才确认文本积分。下游失败或进程在两步之间退出会扩大文本 `held` 窗口。

## 最小修复

1. 两条图片创建路径在预扣成功后，同事务把 `credit_reservation_id` 与实际计费模型同步到对应异步任务。
2. 三条“文本提示词 -> 图片任务”路径在文本结果形成并保存后立即确认文本预扣；下游图片任务失败只影响独立的图片预扣。
3. 增加回归测试，证明：
   - 图片记录与异步任务持有同一个预扣 ID；
   - 服务重启清理能退款明确中断的图片预扣；
   - 文本成功、下游图片提交失败时，文本预扣仍为已确认而不是退款或 `held`。

## 交付门槛

- 相关定向测试全部通过。
- 后端完整测试全部通过。
- 变更范围审计只包含计划内文件。
- 增量发布后茉莉服务健康、AI 音乐 PID 不变、线上 15 条历史 `held` 不被自动修改。

## 验证记录

- 定向回归：`40/40` 通过。
- 后端完整回归：`601/601` 通过。
- `git diff --check`：通过，仅有工作区换行符提示，无空白错误。
- 线上发布与发布后只读核验：待本次增量发布完成后补记。
