# 画布生成历史与设置持久化

## 范围

- 将网格、缩略图、吸附三项画布偏好保存到现有 `canvas_layout`。
- 将成功和失败的节点运行结果保存到现有 `canvas_layout`，每个画布最多保留 100 条。
- 不新增数据库表、接口或全局存储，不改变运行中任务的现有恢复逻辑。

## 验收标准

1. 刷新或重新登录后，画布偏好从后端恢复。
2. 成功、失败记录刷新后仍可查看，运行中的临时记录不写入历史。
3. 历史记录按画布文档隔离，不跨项目或用户混用。
4. 重复保存不会产生重复历史，历史条数有上限。
5. 持久化工具回归测试和前端生产构建通过。
