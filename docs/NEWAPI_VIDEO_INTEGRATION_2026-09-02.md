# NewAPI 视频模型接入与真实生成验证（2026-09-02）

## 范围与结论

- 服务地址：`https://newapi.megabyai.cc`
- 接入协议：`POST /v1/videos`，`GET /v1/videos/{taskId}`，结果下载 `GET /v1/videos/{taskId}/content`
- 目标密钥已用于真实生成，但密钥本身不写入本文档或代码仓库。
- 2026-09-02 首轮仅下表 5 个模型真实生成成功；2026-09-03 重新验证后，`alibaba/wan-3.0` 也已完成真实生成并通过文件校验，当前共有 6 个满足生产接入证据要求的模型。
- `aibaba/wan-3.0` 是错误拼写，`wan3.0-video` 仍未通过，不得写入本 NewAPI 配置。

## 真实生成与文件校验

| 模型 | 任务 ID | 供应商终态 | 实测响应时间 | 供应商计费 | 文件校验 |
| --- | --- | --- | ---: | ---: | --- |
| `seedance-2.0-fast` | `vid_7eb8e5b036e41334aca92f403c3b98b0` | `completed` | 204 秒 | ¥1.00 | 854,056 B；H264/AAC；864×496；5.04 秒；SHA-256 `75114163ac092d18c19f6391cfba443bd7001b5f39145f0a623af57e7358dd33` |
| `seedance-2.0` | `vid_7dba34568e47a6f71ae7d5e6f579ff61` | `completed` | 226 秒 | ¥0.80 | 919,874 B；H264/AAC；864×496；5.04 秒；SHA-256 `89b4e35e9f841abfe20f6accb22596e1d9b10b0afc3b0ea6a6b865c15c68b0b6` |
| `seedance-2.0-mini` | `vid_57b7d8090ddd8e212de843acc3638828` | `completed` | 233 秒 | ¥0.20 | 1,039,774 B；H264/AAC；864×496；4.04 秒；SHA-256 `40200306e6faa489fdd2ac17e3911bb1112503ed652b16663259d8c81b3bae74` |
| `seedance-2.5` | `vid_9798aa3ee9e6e100de256904deb8ce5b` | `completed` | 318 秒 | ¥1.30 | 3,081,041 B；H264/AAC；854×480；5.04 秒；SHA-256 `124e2efbb1bad2aefa70652c05dfd2887fbafcb004ecdcc8b3665d6fe4039c1e` |
| `minimax_h3_image_audio_to_video_v2` | `a85eeae3-9119-47fb-848c-7239d02b5529` | `completed` | 153 秒 | ¥0.15 | 1,680,290 B；H264/AAC；1344×768；5.17 秒；SHA-256 `116d8e1a6f5d8483ec0a668d62fed7f5d796720e16964f4e436a923cb21215ae` |

所有文件已在独立复核中重新计算 SHA-256，并经 `ffprobe` 确认为 MP4 容器、H264 视频和 AAC 音频，复核退出码为 `0`。

## `alibaba/wan-3.0` 重新验证（2026-09-03）

- 实时 `GET /v1/models` 已返回精确 ID `alibaba/wan-3.0`；错误拼写 `aibaba/wan-3.0` 仍不存在。
- 最小真实请求：`POST /v1/videos`，`model=alibaba/wan-3.0`、`duration=4`、`ratio=16:9`、`resolution=480p`，纯文本提示词且不带参考素材。
- 任务 ID：`vid_8a739f6274b99ec5e9bdd8ccef5a1334`；终态 `completed`、进度 100，创建至完成 189 秒。
- 供应商结算：`billing_amount=0.6`、`billing_quota=300000`、`billing_status=settled`，与客服报价 480p ¥0.15/秒 × 4 秒一致。
- 内容接口返回 HTTP 200、`video/mp4`；文件 2,097,217 B，SHA-256 `ad56333bb1cdaffc1f7267e97d6fcf3c810416e2288de97d1450492ba8781c0d6`。
- 本机独立 `ffprobe`：MP4 容器，H264 832×480、30 fps，AAC 音频，时长 4.04 秒；2 秒处抽帧可正常解码并呈现提示词要求的山地日出画面。
- 验证文件：`C:\Users\canqu\AppData\Local\Temp\moli-wan3-verify-20260903\alibaba-wan-3.0-480p.mp4`；密钥未写入本文档或仓库。

本节只证明该模型在上述 480p/4 秒/纯文本组合下可用，不把单次响应时间视为 SLA，也不外推未实测的 720p、1080p 或参考素材组合。

## Wan 3.0 生产接入边界（2026-09-03）

- NewAPI 生产配置固定使用精确模型 ID `alibaba/wan-3.0`，继续保留现有密钥、默认模型和原 5 个模型。
- 画布目录只开放已实测的 `480p`、4 秒、16:9、纯文本生成；代码会拒绝 720p/1080p、其他时长/画幅及参考素材请求，避免把报价能力误当成已验证能力。
- 用户收费沿用平台现有 Wan 3.0 定价：134 积分/秒；本次只创建 480p 档位，不开放未经真实生成验证的分辨率。
- 上游成本以中转站 `GET /api/pricing` 为准。`GET /api/status` 当前声明 `display_in_currency=true` 且 `quota_display_type=CNY`，因此报价是人民币，禁止再次乘美元汇率；Wan 3.0 的已验证 480p 成本为 ¥0.15/秒。
- 中转站同时公布的 720p ¥0.30/秒、1080p ¥0.67/秒仅作为管理员成本信息保存，不代表平台已开放对应生成能力。
- 配置写入采用配置 #29 的 `updated_at` 条件更新并置于单一事务中；发现管理员并发修改时必须零写入并停止。

## 已验证的生产响应能力

下表只描述平台当前允许用户提交、且已有真实成功文件证据的组合；中转站报价页宣称但未实测的其他能力不在生产目录中公开。

| 精确模型 ID | 允许请求 | 参考素材 | 已验证响应 | 单次观测时间 |
| --- | --- | --- | --- | ---: |
| `seedance-2.0-fast` | 480p、5 秒、16:9 | 不允许 | 异步任务完成；MP4，H264/AAC | 204 秒 |
| `seedance-2.0` | 480p、5 秒、16:9 | 不允许 | 异步任务完成；MP4，H264/AAC | 226 秒 |
| `seedance-2.0-mini` | 480p、4 秒、16:9 | 不允许 | 异步任务完成；MP4，H264/AAC | 233 秒 |
| `seedance-2.5` | 480p、5 秒、16:9 | 不允许 | 异步任务完成；MP4，H264/AAC | 318 秒 |
| `minimax_h3_image_audio_to_video_v2` | 768p、5 秒、16:9 | 必须且只能传 1 张参考图；不允许视频或音频参考 | 异步任务完成；MP4，H264/AAC | 153 秒 |
| `alibaba/wan-3.0` | 480p、4 秒、16:9 | 不允许 | 异步任务完成；MP4，H264/AAC | 189 秒 |

响应时间是单次真实生成观测，不是 SLA 或稳定性能承诺。

## 未通过项

- `wan3.0-video`：创建返回 `503 model_not_found`，request id `20260902110828299718883IpKe2OE7`。
- `aibaba/wan-3.0`：模型目录无该 ID；创建返回 `503 model_not_found`，request id `202609021125459454536625FcBl11m`。
- `alibaba/wan-3.0`：2026-09-02 当时模型目录无该 ID，创建返回 `503 model_not_found`，request id `202609021129168042286304T7MZdFL`；该历史失败已被 2026-09-03 的成功复验取代。

除已在 2026-09-03 重新验证成功的 `alibaba/wan-3.0` 外，其余失败请求没有任务 ID，也没有成功生成或可读结果文件，因此不满足生产开放条件。

## 生产上线与管理员验收（2026-09-03）

- 最终生产版本：`/opt/moli-drama/releases/newapi-wan3-20260903-b41-r6`；共享发布审计 `/opt/moli-drama/shared/release-audit/protected-release-20260903T060246Z-1690453.audit` 记录 `activation_success`。
- 激活前数据库备份为 `/root/data/disk/moli-drama-backups/database-release-guard-20260903T060246Z-1690453.sqlite`，SHA-256 `595450c1fb872b74585978cfbb54407451e1a2a9f3b592b38b5d870837db841d`，`quick_check=ok`；切换前后活动生成任务均为 0，停机窗口 200ms。
- 线上运行时收据 `/opt/moli-drama/shared/release-audit/newapi-wan3-runtime-acceptance-20260903T060756Z-r6.json` 为 `ready=true`：画布仅公开 480p、4 秒、16:9、无参考素材，用户价格 134 积分/秒；适配器在本地拒绝 720p、5 秒、9:16 和参考图请求。
- 管理员直达 `/billing-admin?tab=models` 可正常打开，共 12 个中转站队列；NewAPI 只出现 1 个队列并包含 6 个模型。Wan 3.0 卡片只显示 480P 用户收费和 480P 成本输入，不再把 720P/1080P 中转站报价误显示为已开放档位。
- 从模型配置 #29 点击“设置定价”后，地址为 `/billing-admin?tab=models&config_id=29`，只显示 NewAPI 的 1 个队列和 6 张模型卡，未混入其他中转站；模型配置页存在精确 ID `alibaba/wan-3.0`，不存在错误拼写 `aibaba/wan-3.0`。
- 上线后内网健康检查与公网首页、运营计费、模型配置均返回正常，服务 `NRestarts=0`，严重错误日志为空；AI 音乐 `server.js`/`worker.js` PID 仍为 1592199/1592245。

## 六模型首页与短剧工厂修复候选（2026-09-03）

生产复验发现上一版只把 Wan 3.0 公开到了首页/短剧工厂，其余五个已验证模型仍缺少可公开价格；同时页面写死了画幅选项，MiniMax 的 768P 无法进入视频价格表，同名模型创建后也可能失去中转站配置身份。

本次候选按“只开放真实成功请求，不把报价能力外推成已验证能力”收口如下：

| 精确模型 ID | 首页/短剧工厂开放组合 | 用户收费 | 已核对中转站成本 |
| --- | --- | ---: | ---: |
| `seedance-2.0-fast` | 480P、5 秒、16:9、纯文本 | 175 积分/秒 | ¥0.20/秒 |
| `seedance-2.0` | 480P、5 秒、16:9、纯文本 | 140 积分/秒 | ¥0.16/秒 |
| `seedance-2.0-mini` | 480P、4 秒、16:9、纯文本 | 44 积分/秒 | ¥0.05/秒 |
| `seedance-2.5` | 480P、5 秒、16:9、纯文本 | 228 积分/秒 | ¥0.26/秒 |
| `minimax_h3_image_audio_to_video_v2` | 768P、5 秒、16:9、必须一张参考图 | 27 积分/秒 | ¥0.03/秒 |
| `alibaba/wan-3.0` | 480P、4 秒、16:9、纯文本 | 134 积分/秒 | ¥0.15/秒 |

前五个模型沿用平台现有的向上取整定价规则；Wan 3.0 保留已上线的 134 积分/秒。中转站还报价的其他分辨率、时长、画幅和参考素材数量继续仅作为管理员成本信息保存，不向用户开放。

实现门禁：

- 配置 29 的同名模型使用 `cfg-29::模型ID` 计费身份，任务入库同时固定 `ai_service_config_id=29`，轮询和最终结算不再换到其他中转站。
- 六个模型必须同时具备真实生成证据、`validated=true`、正数用户价格和对应 `relay_auto` 分档成本，任一不匹配则整个配置事务零写入。
- 首页与 `/factory` 复用同一模型目录；画幅、时长、分辨率和参考图必填状态均由该模型公开能力生成。独立创作页再次校验，旧草稿不能绕过限制。
- `model_resolution_prices` 增加 768P 合同并保留已有 480P/720P/1080P 数据。
- 上线前只读日志复核发现 4 秒 Wan 3.0 请求仍会先后被视频任务入口和计费层的通用 5 秒下限拒绝；新增回归覆盖两层校验，`alibaba/wan-3.0` 现按实测合同允许 4 秒，其余普通模型仍保持 5 秒下限。
- 生产数据库副本复核又发现 Fast/Mini 因历史停用配置保留同名模型而使用 `cfg-29::` 计费身份，但活动目录曾回退到无前缀身份，导致两者有价格却不公开；目录现优先采用配置 29 已存在的限定价格身份，六模型副本目录恢复为 6/6。

本地候选验证：

- 后端六模型、线路固定、成本同步、计费、功能锁与模型 UI 保护等 11 个测试文件共 `215/215` 通过；模型 UI 保护门禁单独 `35/35`、功能锁与增量范围门禁 `62/62` 通过。
- 前端首页、短剧工厂与计费分组定向测试 `30/30` 通过，生产构建成功。
- 前端全量 Node 测试 `973/974` 通过；唯一失败仍是未改动的飞拓预设静态断言，和本次 NewAPI 运行文件无关。
- 功能锁保留原批准历史，并以用户 2026-09-03 的“继续推进”确认追加 `newapi-six-model-public-remediation` 解锁记录；共享功能锁审计返回 `ready=true`。
- 本节仍是本地候选事实；必须等待并行生产发布窗口释放，从新的实时 `current` 制作候选、通过共享门禁并完成管理员登录态验收后，才能标记为生产交付。
