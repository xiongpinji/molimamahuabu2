# LibTV 对齐多智能体交付计划（2026-07-23）

## 本轮并行交付批次（重新启动）

- 启动基线：`0ccdb9f12083b1f731bb17cf011cec59d7ca6e7c`
- 分支：`codex/libtv-frontend-alignment`
- PR：`https://github.com/xiongpinji/molimamahuabu2/pull/1`
- 当前 staged 区必须保持为空；Agent A–E 不执行 `git add/commit/push`，只在各自边界内修改和验证。
- Agent F 在 A–E 工作期间只做只读基线审计；待根代理完成冲突审查后，再执行提交候选审计和 PR 验收。
- 所有 Agent 都必须保留用户与其他 Agent 的现有修改，不得回退、格式化或清理无关文件；共享文件发生碰撞时停止修改并报告。

本轮六路成功标准：

1. Agent A：补齐右键菜单、空白添加、复制、插入/追加、快捷键、Ctrl 缩放、Space 平移、分组选择的真实浏览器回归。
2. Agent B：统一验证文本、图片、视频、音频、角色、脚本节点的配置、模型、素材、提示词与运行入口；仅修复真实缺口。
3. Agent C：形成图片、视频、首尾帧、音色提示词、失败写回、重试恢复的同链证据；付费调用未经明确授权保持 `blocked`。
4. Agent D：验证素材筛选/复用/回填/历史、音色入库与角色固化；明确区分角色音色与混合音轨。
5. Agent E：验证镜头、序列、切点/转场、角色轨道、动作片段的编辑与保存/恢复；仅修复最小缺口。
6. Agent F：核对阶段边界、测试证据、staged diff、提交、push、PR 与 CI，输出仍未完成的生产差距。

### 本轮执行结果

- Agent A：补齐并验证节点右键复制、追加、插入、下游重连与刷新恢复；Playwright 单文件重复 3 轮，`21/21` 通过。
- Agent B：节点配置矩阵复核通过；针对性测试 `18/18`，前端全量 `257/257`，build 通过；未为凑提交扩大代码。
- Agent C：生成链路定向测试 `57/57`、后端全量 `257/257`；本地兼容 provider 的图片工件、数据库回读与失败写回有效。未获外部付费调用授权，真实外部供应商同链证据继续标记 `blocked`。
- Agent D：素材/音色后端测试 `28/28`、前端测试 `41/41`、build 通过；多人对白与配乐混合且无可靠切点时会拒绝错误绑定。
- Agent E：导演台前端测试 `54/54`、后端测试 `6/6`、Playwright `8/8`、build 通过；专业 NLE、复杂骨骼重定向和电影级离线渲染仍不在当前最小闭环内。
- Agent F：只读基线与提交边界审计完成；仅选择性提交 Agent A 的单个 E2E 文件，未混入历史脏改。
- 根代理复核：前端全量 `257/257`、后端全量 `257/257`、生产 build 通过。

阶段交付：

- 提交：`58a99d76e4d521c9dfe63a8d6ad1977fac171026`（`test(画布): 补节点复制插入追加回归`）
- PR 评论：`https://github.com/xiongpinji/molimamahuabu2/pull/1#issuecomment-5059175929`
- GitHub Actions：`Backend regression` 与 `Canvas browser regression` 均成功。
- staged 区提交后为空；`.omx/**`、`frontweb/.playwright-cli/**`、`03_visual_spec/**`、`test-results/**` 及其他历史脏改均未进入提交。

## 交付目标

在 `codex/libtv-frontend-alignment` 分支上，以可回归、可审计、可演示为标准补齐项目画布闭环。不得用静态界面、源码正则测试或模拟返回替代真实运行证据。

## 实时基线

- 权威仓库：`research/libtv-open-source-audit/repos/LocalMiniDrama`
- 起始提交：`30d45e5ae55d627d603c8025d95d431d2cdd6ec1`
- PR：`https://github.com/xiongpinji/molimamahuabu2/pull/1`
- 当前工作树存在大量历史与并行改动；禁止 `git add .`，仅按阶段选择性暂存。

## Agent 分工与验收

### Agent A：画布交互对齐

- 范围：右键菜单、空白添加节点、节点复制、节点插入/追加、快捷键、Ctrl 缩放、Space 平移、分组选择。
- 主要所有权：`DramaCanvas.vue`、纯画布交互组件、`canvasInteraction*` 与节点结果可视化测试。
- 验收：针对性交互测试、前端全量单测、前端 build、无新增浏览器运行时错误。

### Agent B：节点配置面板闭环

- 范围：文本、图片、视频、音频、角色、脚本节点的统一配置；模型选择、素材挂载、提示词编辑、运行入口。
- 主要所有权：`components/dramaCanvas/*Panel.vue`、相关 composable、`canvasNodeConfigPanel.test.js`。
- 验收：配置面板回归测试、前端全量单测、前端 build。

### Agent C：真实生成链路验证

- 范围：图片生成、视频生成、首尾帧、音色提示词、失败写回、重试恢复。
- 证据分级：`implemented`、`backend_readback`、`artifact_verified`、`failure_writeback`、`blocked`。
- 验收：本地兼容 provider 或真实 provider 的请求、任务轮询、数据库回读与本地工件；外部付费 provider 无额度时必须标为阻塞，不得伪报成功。

### Agent D：素材库与音色库闭环

- 范围：素材筛选、复用、回填、历史调用；音色提取入库、角色音色固定、模型不支持克隆时提示词补偿。
- 主要所有权：素材/音色组件、独立 asset/voice 服务与测试。
- 验收：前后端针对性测试、持久化回读、前端 build；明确区分角色音色与整段混合音轨。

### Agent E：3D 导演台实用化

- 范围：镜头实体、镜头序列、切点/转场、角色轨道、动作片段编排。
- 主要所有权：导演台组件、数据结构、独立工具与 `director*` 测试。
- 验收：UI 入口、编辑、保存、刷新恢复；前端导演测试、后端持久化测试、前端 build。

### Agent F：交付审计与 PR 管理

- 范围：只读审计、阶段候选文件、冲突检查、选择性 stage/commit/push、PR 更新、最终全量验收。
- 排除：`.omx/`、`frontweb/.playwright-cli/`、无关视觉规格和历史脏改动。
- 验收：每次暂存文件清单与阶段范围完全一致；提交前检查 staged diff；推送后等待 CI；最终输出剩余差距清单。

## 阶段提交顺序

1. 画布运行时与交互回归。
2. 节点配置面板。
3. 素材库与音色库。
4. 3D 导演台数据与持久化。
5. 真实生成链路必要修复与验证报告。
6. 最终全量测试、浏览器验收、交付审计文档。

共享文件存在跨阶段修改时，必须拆分 hunk 或暂缓该阶段，不允许用整文件提交掩盖归属。

## 全局门槛

- 前端：全量 `node --test test/*.test.js`、`npm run build`、关键 Playwright 画布回归。
- 后端：全量 `node --test test/*.test.js`。
- 浏览器：项目画布可加载；右键菜单、节点配置、结果预览、失败队列、素材库、导演台入口无新增运行时错误。
- Git：无宽泛暂存；每阶段提交、推送并更新 PR；CI 通过后才进入下一阶段。
- 真实模型：任何可能计费的调用必须有明确授权与额度；无成功工件时保持 `blocked`。

## 执行结果

### 阶段提交

1. `ae3cc11 fix(画布): 兼容失败队列缺失结果引用`
   - 修复失败队列没有 `resultReferences` 时的画布运行时崩溃。
   - 前端针对性测试 46/46、前端全量测试 257/257、build 通过。
2. `86e3442 test(音色库): 补分镜音频挂载回读`
   - 增加音色素材挂载到分镜音频槽位后的 SQLite 持久化回读回归。
   - 音色/素材后端测试 28/28、前端素材/音色测试 41/41、build 通过。
   - 在干净 worktree 中重新安装依赖并验证 `voiceLibraryAsset.test.js` 7/7。

未形成独立净改动的 Agent B、C、E 阶段没有为凑提交而扩大范围：

- 节点配置：现有实现已覆盖文本、图片、视频、音频、角色、场景、道具、脚本节点，以及模型选择、素材挂载、提示词编辑、运行和重试；配置回归 18/18。
- 生成链路：图片、视频、首尾帧、音色提示词、失败写回和重试恢复的后端全量回归最终为 257/257；未执行可能计费的外部供应商调用。
- 3D 导演台：镜头实体、镜头序列、转场、角色动作编排、真实模型/动作 URL、保存恢复入口已存在；导演台前端测试 54/54、后端测试 6/6、Playwright 8/8。

### 根代理独立验收

- 后端全量：257/257，通过。
- 浏览器项目画布：`http://localhost:3013/film/3/canvas` 可加载。
- 右键/添加节点菜单：故事脚本、分镜、3D 导演台、角色、场景、道具、新集、图片、视频、音频、素材库入口可见。
- 缩放与平移：放大按钮从 24% 变为 28%；普通滚轮将视口 `translateY` 从 `551.502px` 改为 `311.502px`，`scale(0.284558)` 保持不变。
- 工作流面板：图像模型、视频模型、画幅、分辨率、时长、生成步骤与运行入口可见。
- 素材库：角色、场景、道具、项目素材以及预览、复制引用、指派入口可见。
- 节点结果：首帧、尾帧、视频状态、预览、复制、下载、下游参考和重试入口可见；失败原因与运行队列可读。
- 3D 导演台：镜头序列、镜头实体、转场与时长、角色动作编排、动作资源 URL、角色轨道和时间线入口可见，保存状态可读。

### PR 与 CI

- PR：`https://github.com/xiongpinji/molimamahuabu2/pull/1`
- 最新验收提交：`86e34425e74d75a19279803c857f8237fefdc9ab`
- PR 状态：OPEN、非草稿、MERGEABLE。
- GitHub Actions：
  - Backend Node Tests：成功。
  - Frontend Canvas E2E：成功。

## 仍需明确的边界

- 本阶段没有获得新的外部付费模型调用授权，因此真实供应商“提交任务 → 轮询 → 成功工件 → 数据库回读”的同一次链路仍标为 `blocked`；本地回归和已有项目工件不能冒充新的付费调用证据。
- 音色提取当前按对白顺序与静音切分处理，不是任意混合音轨的通用人声分离；复杂配乐、环境声或多人重叠对白仍需专门的源分离/说话人分离服务。
- 不支持音色克隆的模型只能使用固定文本音色锚点补偿，无法保证与克隆模型相同的声纹一致性。
- 3D 导演台达到最小可用编排闭环，但尚未证明复杂 GLB/VRM 骨骼重定向、IK、专业 NLE 曲线/多层编辑或电影级离线渲染。
- 当前工作树仍含大量早于本阶段或其他并行任务的脏改动；它们未被本阶段暂存、提交或清理。
