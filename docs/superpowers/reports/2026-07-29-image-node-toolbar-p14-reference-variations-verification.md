# 图片节点工具栏 P14：参考图生成与推演验证记录

## 当前结论

- 画面联想、角度联想、角色三视图、多机位叙事九宫格、画面推演 3 秒后和画面推演 5 秒前已完成本地代码接入与自动验证。
- 六项能力分别显式声明，只允许已审计的 `storyboard_image / volcengine / doubao-seedream-4-5` 参考图适配器。
- 结果均作为派生素材入库，不覆盖源素材。
- 本包没有增加核验、侵权检测、版权判断或对口型。
- 尚未执行隔离浏览器总验收，因此本记录不把 P14 标记为最终发布完成。
- 未推送、未创建 PR、未同步线上服务器。

## 输出约束

- 画面联想、角度联想、前后帧推演：与源图同尺寸 PNG。
- 角色三视图：`2048x1536` PNG，包含正面、侧面、背面和 3/4 视角。
- 多机位叙事九宫格：`3072x3072` PNG，包含连续的 3×3 画格。
- 可选补充要求严格限制为字符串且不超过 300 字。

## 自动验证

- 后端图片工具测试：34/34。
- 前端工具栏测试：15/15。
- 自由画布节点持久化测试：11/11。
- 后端全量测试：465/465。
- 前端全量测试：343/343。
- 前端生产构建：通过。
- 前后端 `npm audit --audit-level=high`：0 个漏洞。
- `git diff --check`：通过。

## 安全与失败验证

- 公开平台模式下六个操作均保持禁用。
- 未声明能力、普通文生图配置、仿冒模型名和未审计供应商不能开放操作。
- 供应商空结果、错误比例、与源图哈希相同的结果和非法参数均拒绝入库。
- 供应商错误正文和测试密钥不进入响应或日志。
- 下载临时文件在成功或失败后均清理。
- 失败重试只持久化白名单内的 `description`，超限时整组拒绝。

## 待总验收

- 隔离端口真实浏览器操作。
- 任务和派生资产后端回读。
- 产物下载、MIME、尺寸和刷新恢复。
- 失败写回与浏览器重试。
- 完成全部非核验功能后的统一许可证、网络行为和禁区审计。
