# 一键转绘阶段 1 验收门禁记录

日期：2026-08-06
分支：`codex/short-drama-redraw-design`
范围：阶段 1 任务 1-7，本地验证；未执行生产部署。

## 已验证

- 阶段 1 后端回归：90/90 通过，覆盖迁移、源片上传、能力目录、源片分析、路由、积分账本、任务恢复和剧本分析路由。
- 前端合同/运行时测试：20/20 通过。
- 浏览器 E2E：`frontweb/e2e/redraw-workspace.spec.js` 桌面 1440x900、移动 390x844 均通过，覆盖入口、项目/作品、MP4 上传、四类风格、自由风格互斥、语言/地区、报价门禁、multipart 分析 payload、任务状态和刷新恢复。
- Playwright CLI 本地页面快照/截图：桌面和移动均完成；DOM 检查确认 `document.documentElement.scrollWidth` 与 `document.body.scrollWidth` 未超过视口。移动端内部导航和风格分段控件允许自身横向滚动，不撑宽页面。
- `npm run build` 通过；仅保留既有大 chunk 警告。`git diff --check` 通过。

## 真实模型门禁

状态：`blocked`

本记录没有目标视频理解模型的 Key、真实源视频生成/分析成功终态、任务 ID、事实层版本、可回读时间码或可读取结果资产证据。当前 E2E 使用浏览器路由夹具，只证明 UI/API 合同和任务状态恢复，不证明供应商模型能力。未满足“真实生成成功且结果文件可读”前，不把任何新增模型写入生产能力目录，也不把阶段 1 标记为真实同链完成。

下一步需要在获得目标 Key 和测试源视频后，执行一次真实分析，记录不含密钥的供应商配置 ID、任务 ID、成功终态、事实层版本、时间码回读和结果文件可读性；失败或结果不可读时继续保持 `blocked`。
