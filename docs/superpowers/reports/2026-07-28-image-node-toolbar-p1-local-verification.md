# 图片节点工具栏 P1 本地验证报告

- 日期：2026-07-28
- 分支：`codex/image-node-tool-suite`
- 设计：`docs/superpowers/specs/2026-07-28-image-node-toolbar-design.md`
- 结论：P1 本地门禁通过；整项功能仍未完成，不创建 PR、不推送、不部署

## 1. 本次完成范围

已接入真实处理链：

- 裁剪：Cropper.js 交互，Sharp 生成派生资产
- 压缩：JPEG / PNG / WebP 格式和质量参数
- 镜像：水平或垂直翻转
- 旋转：顺时针 90°、180°、逆时针 90°
- 宫格裁剪：最多 25 个派生资产
- 图片调整：亮度、对比度、饱和度、色温
- LUT 调色：电影、暖色、冷色、黑白预设
- 替换图片、下载、全屏预览、任务历史、节点标记色
- 运行时锁定可变操作；失败后在节点工具栏显示错误并允许按原参数重试

所有处理均保留源资产，成功后创建新资产并更新图片节点；节点刷新后继续保留图片地址、处理任务、历史与标记色。

## 2. 明确未完成范围

以下入口只展示真实能力状态和未接通原因，不返回模拟结果：

- 标记修图、智能抠图、框选抠图
- 高清增强、细节纹理增强、灯光调整、电影级光影校正
- 720 全景、生成全景场景、扩图
- 姿势、角度、画面联想、角度联想
- 角色三视图、多机位叙事九宫格、前后帧推演、对口型
- 当前图片到 3D 导演台的素材桥接

核验、侵权检测、版权判断、绿盾状态和核验报告仍完全排除。

## 3. 隔离验证环境

- 工作树：`C:\Users\canqu\Documents\茉莉妈妈2\wt-image-node-toolbar`
- 前端：`http://127.0.0.1:3033`
- 后端：`http://127.0.0.1:5699`
- 数据库：`C:\Users\canqu\AppData\Local\Temp\molimama-image-tools-e2e-20260728\drama.sqlite`
- 产物目录：`C:\Users\canqu\AppData\Local\Temp\molimama-image-tools-e2e-20260728\storage`

原工作树的端口、数据库和未提交文件未被修改。

## 4. 自动化门禁

| 门禁 | 结果 |
| --- | --- |
| 后端全量测试 | 441 / 441 通过 |
| 前端全量测试 | 323 / 323 通过 |
| 前端生产构建 | 通过 |
| `git diff --check` | 通过 |
| 后端生产依赖审计 | 0 个漏洞 |
| 前端生产依赖审计 | 0 个漏洞 |

生产构建仍报告仓库已有的大分块告警；Cropper.js 被动态拆分为独立约 38.20 kB 包，本次没有扩大主入口同步依赖。

## 5. 浏览器与后端回读

隔离浏览器完成了以下同链验证：

1. 工具栏只在有图片的图片节点悬停、被选中或工具栏保持焦点时显示。
2. 通过真实文件选择器替换图片，素材入库成功。
3. 压缩与连续镜像均调用后端并生成新素材。
4. 旋转 90° 生成尺寸从 `640 × 420` 变为 `420 × 640` 的新素材。
5. 色温 `0.4` 调整生成新素材，Sharp 任务元数据保存完整参数。
6. 处理历史可回读；刷新页面后仍显示六条已完成记录。
7. 未接通入口均为原生禁用，标题保留后端返回的未接通原因；导演台不再误报可用。
8. 标记色刷新后仍为 `#a1a1aa`。
9. 将隔离素材临时移出产物目录后执行镜像，节点保留原图并显示“源图片文件不存在”和重试入口；恢复同一素材后点击重试，成功生成新资产并清除错误与重试状态。
10. 浏览器控制台只有 Vite 调试日志，无错误或警告。

数据库和文件系统回读：

- 8 个素材记录对应文件全部存在。
- 最新产物为 WebP，尺寸 `420 × 640`，镜像参数为 `horizontal`，并保留上一项色温 `0.4` 调整产物。
- 最新 URL 只有一层 `derived`，连续处理不再产生嵌套目录。
- 7 个成功任务均为 `completed / 100%`。
- 人为提交越界裁剪后，任务写回 `failed` 和“裁剪范围超出源图片”，且未产生对应派生素材。
- 临时缺失源文件在任务创建前即被拒绝，但前端仍持久化失败操作和原参数；素材恢复后的真实重试形成第 8 个派生素材。

## 6. 审计修正

第一轮规范与规格审计发现的实质问题已处理：

- 不可用入口从“灰显但可点击”改为原生 `disabled`。
- 处理进行中禁用工具菜单、设定菜单、替换、标记和其他处理操作。
- 失败状态保存原操作与参数，工具栏展示错误和“重试”入口；成功后清除重试状态。
- 补齐旋转与色温真实 Sharp 处理链。
- “生成导演台”在当前图片素材桥接完成前保持禁用。

文件命名继续匹配本目录既有的 Vue 组件和 JavaScript 模块风格，未为了局部功能扩大为全仓命名重构。

## 7. 许可证记录

| 依赖 | 版本 | 许可证 | 用途 |
| --- | --- | --- | --- |
| Cropper.js | 1.6.2 | MIT | 浏览器裁剪交互 |
| Sharp | 0.35.3 | Apache-2.0 | 本地确定性图片处理 |

P2 及后续模型不得沿用本表结论；每个源码和模型权重必须单独审计后才能启用。

## 8. 当前交付判断

P1 同时具备真实实现、后端回读、产物验证、失败写回、浏览器验证和依赖许可证记录，可以作为本地阶段性提交。

整项图片节点补齐仍为未完成：P2、P3、P4 尚未通过真实能力门禁，因此本报告不支持创建 PR、推送或同步线上服务器。
