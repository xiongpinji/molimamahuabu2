# 图片节点工具栏：非核验功能汇总验收

## 验收范围

- 本轮只验收图片节点工具栏的非核验功能。
- “核验 / 侵权检测 / 版权判断”仍是明确禁区，没有进入产品代码。
- 用户已于 2026-07-29 暂停对口型；`lip_sync` 保持不可用，不计入本轮完成门槛。
- 本报告只记录本地开发、验证和审计，不代表已推送 PR 或同步线上服务器。

## 自动化验证

| 项目 | 结果 |
| --- | --- |
| 后端完整测试 | 465 / 465 通过 |
| 前端完整测试 | 343 / 343 通过 |
| 前端生产构建 | 通过 |
| 后端依赖审计 | 0 个漏洞 |
| 前端依赖审计 | 0 个漏洞 |
| 生产代码禁区扫描 | `核验 / 侵权 / 版权 / copyright / infringement` 命中 0 |
| `git diff --check` | 通过，仅有工作区换行符提示 |

## 隔离浏览器验收

验收环境使用独立端口、独立 SQLite 数据库和独立存储目录：

- 前端：`127.0.0.1:5699`
- 后端：`127.0.0.1:3033`
- 画布项目包含 9 个节点，其中 1 个图片验收节点、8 个项目资产节点。
- 图片节点可选中，工具栏和菜单正常展开。
- 浏览器控制台 warning / error 数量为 0。

当前能力清单符合严格门控：

- 可用：裁剪、压缩、镜像、旋转、宫格裁剪、图片调整、LUT 调色、灯光入口、导演台、姿势、角度。
- 不可用并显示真实原因：标记修图、智能抠图、框选抠图、高清放大、细节增强、电影级光影校正、720 全景、全景场景、扩图、画面联想、角度联想、角色三视图、多机位叙事九宫格、前后画面推演。
- `lip_sync` 明确不可用，原因是“对口型模型能力尚未配置”。

上述结果证明未配置能力不会落入空处理器、假成功或静态模拟，也没有影响已有确定性工具和导演台入口。

## 当前发布判断

`productComplete=false`

原因：

1. P13 / P14 的实现和自动化测试已通过，但本次汇总验收时，先前隔离使用的 Seedream 提供方配置已被清理，无法重新完成同一真实提供方的浏览器调用、后端回读、产物验证和失败回写闭环。
2. 当前能力接口因此正确地将相关模型操作标记为不可用；不能用禁用态或历史报告冒充本次真实链验证。
3. 对口型已按用户要求退出当前范围；核验功能仍未启动。

在补回经审计的真实模型配置并取得新的同链证据前，不创建 PR、不推送、不部署线上服务器。
