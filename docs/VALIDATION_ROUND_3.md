# LibTV 画布 UI 首轮验证记录

日期：2026-07-15
范围：仅验证 “茉莉妈妈短剧制作平台” 的画布工作区 UI 与交互壳层，不调用任何付费模型。

## 已完成

- 深色浮层式画布顶栏，保留工作区 / 画布名称，并提供工作流面板开关与分享入口。
- 工作流、生成控制面板改为可折叠浮层，避免占满画布。
- 左侧素材库改为可开关浮动面板，画布保持全宽。
- 底部 LibTV 风格浮动工具栏：添加节点、工作流、素材库、剧本、整理、帮助、列表模式、缩放与适应视图。
- 工具按钮增加中文提示、键盘可访问名称和不小于 44px 的触控目标。
- 对齐、适应视图、缩放状态接入现有 Vue Flow 上下文，不新增后端接口。

## 修改文件

- `frontweb/src/views/DramaCanvas.vue`
- `frontweb/src/components/dramaCanvas/CanvasFloatingToolbar.vue`
- `frontweb/src/components/dramaCanvas/CanvasFlowAligner.vue`
- `docs/LIBTV_CANVAS_UI_ROUND_1.md`

## 验证结果

- `npm run build`：通过，Vite 完成 1799 个模块构建。
- `node --test test/*.test.js`：25/25 通过，0 失败。
- `git diff --check`：未发现空白错误；仅有 Windows 换行提示。
- 未执行真实付费模型调用，未触发积分扣费。

### 2026-07-15 浏览器冒烟（3013）

- 首页品牌、快速开始和“首页自由画布”入口可用，独立画布明确显示“不绑定项目”。
- 画布顶栏、底部工具栏、帮助提示和顶部“添加节点”入口可见；帮助提示确认空格拖动、Ctrl+滚轮缩放、普通滚轮滚动及右键添加节点。
- 空画布点击“适配画布”后仍显示 200%，符合当前没有节点可拟合的预期；缩小按钮可改变缩放状态。
- 分享按钮可点击，但本次未观察到可见提示或剪贴板结果，因此不把分享成功记为已验证。
- 右键空白处可打开“在此添加”，选择“文本节点”后可进入“添加节点”对话框。
- 本地前端代理到 `127.0.0.1:5679` 未启动，项目列表与集数 API 仍未验证；本轮未调用付费模型。

### 2026-07-15 集数上下文静态审计

- 选择具体集数时，画布图与素材库共用同一集数上下文，只保留该集分镜实际引用的角色、场景和道具。
- 切回“全部集数”时恢复项目级素材，并保留所有集的分镜节点与关系。
- 素材高亮在切换集数后会清除已不可见的资源，避免关系高亮指向当前上下文之外的节点。
- 新增 2 个纯函数测试覆盖当前集过滤、全部集回退和资源可见性判断。

## 尚未纳入本轮

- 项目级 `user_id` 归属与所有项目 API 的完整隔离。
- `/static` 私有资源鉴权与生产反向代理策略。
- 真实浏览器截图级视觉验收已覆盖首页入口与独立画布基础操作；项目列表 API 与分享结果仍待后端可用后复验。

## 下一步建议

下一步先做无后端场景下的静态归属审计，明确“当前集 / 全部集”对画布节点、素材库和列表模式的上下文边界；后端恢复后再复验项目列表、分享结果与项目级鉴权。
