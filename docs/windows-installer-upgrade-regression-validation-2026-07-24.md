# Windows 安装与升级回归验证（2026-07-24）

## 结论

本阶段验收通过。Windows 安装身份恢复为既有 `com.localminidrama.desktop`，可见品牌保持“茉莉妈妈短剧制作平台”；当前包安装/覆盖/卸载进入 CI，旧版升级进入可传入基线安装器的发布前回归。

## 真实安装链路

- 当前安装包：`茉莉妈妈短剧制作平台 1.2.8 Setup.exe`
- 当前安装包 SHA-256：`1AA5388752890DAC547A98AAC7930FA0DEBDD2C5485C3F900DE19EF86561A574`
- 历史基线：提交 `c33487c` 构建的 `LocalMiniDrama 1.2.8 Setup.exe`
- 历史基线配置：`appId=com.localminidrama.desktop`、`productName=本地短剧助手`
- 升级结果：历史基线安装成功，当前安装包覆盖成功，当前主程序产品名和文件说明均为“茉莉妈妈短剧制作平台”，文件版本为 `1.2.8`
- 数据结果：覆盖安装后用户数据哨兵存在；卸载后用户数据哨兵仍存在
- 清理结果：卸载后主程序和卸载器均消失，无对应 HKCU 卸载注册项，无 `molimama-installer-validation-*` 临时目录，无 `.release-validation` 残留

## 失败路径验证

通过传入故意错误的产品名制造覆盖安装校验失败。脚本按预期失败，同时调用实际卸载器完成清理；验证后无卸载注册项和临时目录残留。

## 自动化门禁

| 验证项 | 结果 |
| --- | --- |
| `node --test test/desktopReleaseWorkflow.test.js` | 5/5 通过 |
| PowerShell AST 语法检查 | 通过 |
| 后端 `node --test test/*.test.js` | 391/391 通过 |
| 前端 `node --test test/*.test.js` | 288/288 通过 |
| 前端 `npm run build` | 通过；仅有既存的大分块提示 |
| 桌面 `npm run dist -- --publish never` | 通过 |
| 同包静默安装、覆盖安装、卸载 | 通过 |
| 历史基线到当前品牌版本的覆盖升级 | 通过 |
| 失败路径卸载与残留清理 | 通过 |

## CI 行为

Windows 验证包构建完成并生成校验和后，工作流会调用 `desktop/scripts/validate-windows-installer.ps1`。CI 默认执行当前包的安装、重复覆盖和卸载，避免依赖可能过期的历史产物；发布候选验证可通过 `-BaselineInstallerPath` 增加指定历史安装包到当前安装包的升级验证。

## 提交前审计

- 规范轴未发现硬性违反；证据文档按仓库规范改为 kebab-case 文件名。
- 规格轴指出文件说明和卸载器消失断言不足，现已补齐并重新通过真实升级回归。
- CI 与发布前旧版升级的边界已写入计划和验证记录，避免把本地历史基线证据误报成 CI 证据。

## 已知非阻断项

- 当前验证包明确为未签名构建；正式标签发布仍由既有签名门禁负责。
- 前端生产构建存在既有大分块警告，本阶段未修改前端业务代码，也未扩大范围处理该提示。
