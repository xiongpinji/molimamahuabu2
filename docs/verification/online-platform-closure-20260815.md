# 线上平台一次性验收矩阵（2026-08-15）

> 分支：`codex/online-platform-closure-20260815`
> Git 基线：`origin/main@38135ad1`
> 线上版本：`/opt/moli-drama/releases/platform-stability-pr149-20260815-38135ad1-gatefix2`
> 原则：历史成功、页面存在、单元测试或模拟供应商均不能替代本批次真实闭环。

## 证据等级

- `pass-ui`：本批次真实线上页面完成非写入交互，且没有应用错误。
- `pass-existing-artifact`：本批次打开并读取了线上既有成功工件，但没有新提交付费任务。
- `pass-test`：同一 Git 基线的自动化合同通过；不能等同于线上供应商成功。
- `fail`：本批次已复现缺陷。
- `blocked`：缺少单次付费授权、可验证供应商能力或安全发布基线，不能安全执行。

## 画布

| 能力 | 本批次证据 | 状态 | 结论 |
|---|---|---|---|
| 页面与节点图 | 已登录打开 `/canvas/63`，现有文字、图片、视频节点及连线可读取 | `pass-ui` | 页面主体正常 |
| 3D 导演台入口 | 点击底部“导演台”，`ThreePipe Viewer 0.5.1` 初始化；活动对话框、场景树、视角切换、变换工具、相机、灯光、属性检查器均存在 | `pass-ui` | 导演台已实际打开；浏览器截图接口未合成浮层，但 DOM 顶层命中和可见性均正常 |
| 导演台只读交互 | “导演视角/机位视角”切换、帮助打开/关闭、导演台关闭均成功 | `pass-ui` | 未修改场景对象，未触发自动保存 |
| MiniMax H3 多参考 | 线上失败任务以 15 秒、1440P、4 张实际参考图提交；供应商受理后返回参数校验失败 | `fail` | 页面宣称/目录/请求构造的参考上限不一致；必须在扣费提交前阻断未验证组合 |
| MiniMax H3 成功边界 | 同批次只读回查：15 秒、1440P、3 张参考图已有成功结果；4 张失败 | `pass-existing-artifact` | 在没有新的单次付费证据前，安全上限只能锁到 3 张 |
| 视频理解 | 生产没有 `video_understanding` 已验证配置 | `blocked` | `VIDEO_UNDERSTANDING_NOT_VERIFIED` 是正确保护，禁止改开关绕过 |
| 新图片/视频/音频/文本生成 | 本批次未提交新付费任务 | `blocked` | 不能据此宣称所有线上模型当前可用 |

## 短剧工厂

| 能力 | 本批次证据 | 状态 | 结论 |
|---|---|---|---|
| 工厂项目与剧集 | 打开既有项目 `[全流程实测] 2026-08-01 小橘猫回家` 与剧集 27 | `pass-ui` | 路由、项目和剧集读取正常 |
| 制作页资源 | 剧本、角色、道具、场景、分镜脚本、分镜图、分镜视频均可读取 | `pass-existing-artifact` | 既有工件完整，不代表本批次新生成 |
| 快捷导航 | 收起/展开制作流程成功；点击“分镜视频”后视口定位到合成视频区域 | `pass-ui` | 导航交互正常 |
| 成片预览 | 现有 30 秒合成视频控件可见、地址已由页面正常加载 | `pass-existing-artifact` | 现有成片可用 |
| 导出入口 | Excel 与 SRT 导出按钮可用 | `pass-ui` | 尚未把下载文件内容纳入本批次证据 |
| 新的一键成片/批量生成/配音 | 本批次未提交付费任务 | `blocked` | 不能宣称当前供应商端到端全部成功 |

## 剧本分析

| 能力 | 本批次证据 | 状态 | 结论 |
|---|---|---|---|
| 项目读取 | 打开 `验收-剧本分析-20260801`，当前状态“已通过”，4 个分析版本可见 | `pass-ui` | 项目与版本读取正常 |
| 角色/场景/道具 | 三个标签逐一点击；分别读取母亲/女儿、厨房/车站、信件/列车 | `pass-ui` | 标签与投影数据正常 |
| 导演故事板 | 7 个镜头、图片提示词、视频提示词、连续性和审核问题可读取 | `pass-existing-artifact` | 既有分析工件完整 |
| 保存/重新分析/人工校订/退回/通过 | 会改写生产数据，本批次未执行 | `blocked` | 需要隔离测试项目或明确生产写入授权 |
| 进入画布生产 | 入口存在，本批次未创建新的生产投影 | `blocked` | 不以入口存在代替写回闭环 |

## 已确认缺陷与保护动作

1. `MiniMax H3` 的线上能力合同互相冲突：
   - 参考能力服务仍返回 `maxImageReferences = 4`；
   - 当前供应商客户端声明 `maxImages = 5`；
   - 公开目录因合并顺序同时暴露 `maxImageReferences = 4` 和 `maxReferences = 5`；
   - 本批次线上证据是 3 张成功、4 张失败。
2. 安全修复目标：统一锁定为 3 张，未采用素材不生成 `@图片N`，超限在积分冻结与供应商提交前失败。
3. 生产来源存在血缘分叉：线上相关源文件来自本地生产快照线，而 `origin/main@38135ad1` 仍是旧版 5 秒/480P 合同。直接从 `origin/main` 构建并覆盖会回退线上 15 秒/1440P 及后续视频能力。
4. 因此在修复代码前必须先选择并审计生产源码收敛方式；不能用旧 main 制作候选，也不能把线上文件无审计地反灌主干。

## 运维缺陷

- 归档定时器因为数据盘达到 75% 停止阈值持续失败；数据盘约 153 GB 中，约 147 GB 是 272 份完整历史发布副本。
- 脚本没有检查真实部署锁 `/opt/moli-drama/shared/deploy.lock`，并把 AI 音乐 `/health` 当成茉莉妈妈公共健康检查，遗漏 `/drama-health`。
- 本批次没有删除、压缩、移动归档，没有重启服务或切换版本。

### 本地修复证据

- 已将归档脚本纳入本分支的 `deploy/storage-archive-guard/`，没有替换线上脚本。
- 红灯：`node --test test/releaseArchiveGuard.test.js` 首次执行 2/2 失败，原因是版本化脚本尚不存在。
- 绿灯：归档脚本加入真实部署锁、茉莉妈妈与 AI 音乐双健康检查，并在数据盘达到停止阈值时输出 `ALERT` 后以成功状态停止，2/2 通过。
- Bash 语法检查：`bash -n deploy/storage-archive-guard/moli-drama-release-archive` 通过。

## 生产源码收敛证据

- 2026-08-15 通过只读 SSH 重新确认实时 `current` 仍为
  `/opt/moli-drama/releases/platform-stability-pr149-20260815-38135ad1-gatefix2`；没有创建候选、写数据库、重启服务或调用供应商。
- 受影响运行时依赖按文件从实时 `current`/本地生产源码线收敛。逐文件忽略行尾比较后，14 个运行时文件与实时版本一致，包括 USMercari、Token6688、ToAPIs、灵境、媒体选择、参考能力、自由画布生成、快速生成、时长/分辨率及画布界面。
- `canvasModelCatalogService.js` 在实时版本之上保留 `origin/main@38135ad1` 的两项安全合同：只计算已验证配置，且旧数据库缺少验证字段时才允许环境回退；同时不向逻辑模型泄露供应商上游模型名。
- `aiConfigService.js` 保留生产的逻辑模型/故障切换字段，并保留主干的连接参数变更后自动撤销验证状态、供应商密钥隔离和只读连接探针。
- 收敛红灯首先复现 4 个后端目录兼容失败和 4 个前端依赖/请求合同失败；修复兼容层后，后端定向 14/14、前端定向 26/26 通过。
- 本节只证明源码基线可继续修复，不代表 MiniMax H3 三参考图修复、全量回归、PR/CI 或生产发布已经完成。

## MiniMax H3 三参考图修复证据

- 红灯：H3 客户端仍声明 `maxImages = 5`，4 张参考图请求不会在本地拒绝；目录同时暴露 `maxImageReferences = 4` 与 `maxReferences = 5`。
- 绿灯：唯一供应商规格改为 3 张后，供应商请求构造、视频参考能力服务和公开模型目录均返回 3；3 张请求保留，4 张请求在调用供应商前抛出“最多支持 3 张参考图”。
- 前端采用计划仅启用前三张；请求数组与 `@图片1`、`@图片2`、`@图片3` 共享同一采用序列，第 4 张不提交且不生成提示词 token。
- 定向结果：后端 H3/目录 12/12、前端 H3/自由节点 27/27 通过。

## 平台闭环回归与缺陷修复证据

- 项目画布复制节点后，旧节点曾被自动聚焦的新副本挤出浏览器视口，真实失败坐标为 `y = -66.7`；现改为对旧节点和新副本成对适配视口，并用浏览器实际视口尺寸断言旧节点仍可操作。
- 项目节点编辑器首次展开后，VueFlow 桥接层未暴露 `setCenter` 与 `updateNodeInternals`，导致相机选项和底部保存按钮先后落到视口外；现先重新测量展开后的节点，再按真实尺寸适配视口。
- 视频任务开始时即保存最终解析后的实际模型；后续轮询、刷新恢复和错误审计不再退回用户选择前的旧模型值。
- 真实后端项目画布回归 7/7 通过：图片生成/入库/刷新，视频首帧/失败重试/入库/刷新，节点操作/连线/素材，节点配置保存/刷新，导演台镜头/角色持久化，CC0 角色资产上传/刷新，MP4 转码/登记/下载。
- 项目画布 CI 场景 9/9 通过；画布五文件浏览器组合回归 49 通过、1 个明确标注的付费 AIHubCC 用例跳过、0 失败。跳过项不能作为供应商可用证明。
- 前端全量 Node 合同 657/657 通过，生产构建成功（1858 个模块）；后端全量 850 通过、1 跳过、0 失败。
- `feature-lock-manifest-v1` 与 `canvas-reference-numbered-mentions-v1` 两项共享审计均返回 `ready: true`；提交后仍须针对最终提交重新执行，防止审计基线漂移。
- 本轮没有调用付费供应商，没有写入或重启生产服务，没有修改生产数据库，也没有触碰 AI 音乐 `/opt/moli-mama`。

## 当前发布门禁

### 生产旧库兼容补充

- 首个候选由实时 `current` 克隆后，只覆盖审计白名单文件；组合测试发现线上保留的早期迁移把 `verification_status` 默认值设为 `pending`，而主干新库默认值为 `unverified`。
- 红灯：在默认值为 `pending` 的最小旧库结构中创建供应商配置，实际状态为 `pending`，没有满足“新配置必须先真实验证”的目录合同。
- 修复：创建配置时显式写入 `verification_status = 'unverified'`，不依赖各历史库的列默认值；没有覆盖线上保留的转绘迁移或模型目录实现。
- 绿灯：新旧库兼容测试 8/8 通过；生产候选仍需在最终合并提交上重建并重新通过共享门禁，当前未切换线上版本。
- 首次共享门禁只验证阻止了切换：生产固定审计器要求 `return buildFreeCanvasReferenceMentionCandidates(...)` 后仍能追踪到直连图片收集，主干审计器此前只检查两者同时存在。
- 兼容修复把直连图片收集放回统一候选构造器的返回表达式，同时继续先经过模型能力规划并只为 `adoptedReferences` 生成 `@图片N`；本地审计器同步收紧为生产固定合同且保留“未采用参考图不得生成 mention token”的额外检查。
- 此门禁失败发生在 `PROTECTED_RELEASE_VERIFY_ONLY=1` 阶段，线上 `current`、服务、数据库和 AI 音乐均未切换或重启。

以下为生产切换前的门禁记录：

1. 付费生成项与生产写入项仍缺同批次闭环，因此不能宣称所有外部模型或全部生产写回当前无缺陷；
2. 归档保留策略仍是独立运维决策，不随本次应用发布自动执行；
3. 必须先提交本轮修改，重新执行最终提交审计，检查其他工作树与最新 `origin/main` 冲突，再推送 PR、等待 CI 并合入；
4. 合入后必须重新读取实时 `/opt/moli-drama/current`，从该版本克隆候选，只覆盖审计白名单内的改动，完成部署锁、CAS、备份、活动任务、双服务健康、日志和 AI 音乐隔离检查；
5. 生产切换只能使用共享 `activate-protected-release.sh`，不得整体覆盖或直接替换 `/opt/moli-drama/current`。

## 生产发布与切换后验收

- PR #150、#151、#152 已依次合入；最终运行时提交为 `1a80773c267f348a307b8d8330dcc1f367631c22`。
- 最终候选从实时 `current` `/opt/moli-drama/releases/platform-stability-pr149-20260815-38135ad1-gatefix2` 克隆，只覆盖 6 个审计白名单运行时文件；候选为 `/opt/moli-drama/releases/platform-closure-pr152-20260815-1a80773c-r3`。
- 增量范围清单为 `/opt/moli-drama/shared/release-audit/pr152-platform-closure-1a80773c-scope.json`，SHA-256 为 `5413b54607b24a1b9159966a7682394b7fa02d206b7c211d0e30e8f5c1ce5001`。
- 候选组合回归通过：后端生产目录 3/3、旧库兼容 4/4、视频与目录 32/32；前端 H3 与自由节点请求 27/27，图片/视频/音频参考编辑器定向 1/1；生产环境预检 16/16。
- 共享发布门禁在 verify-only 和正式激活时均通过 `canvas-credit-callout-v1`、`canvas-reference-numbered-mentions-v1` 与外部模型发布合同。
- 2026-08-15T14:03:49Z 使用共享 `activate-protected-release.sh` 开始原子切换，2026-08-15T14:08:39Z 成功切换到最终候选；没有直接替换 `current`。
- 发布审计：`/opt/moli-drama/shared/release-audit/protected-release-20260815T140349Z-3446232.audit`；完整日志：`/opt/moli-drama/shared/release-audit/protected-release-20260815T140349Z-3446232.journal`。
- 数据库备份：`/opt/moli-drama/shared/backups/database-release-guard-20260815T140349Z-3446232.sqlite`，备份 SHA-256 为 `a27ae5321e0900c51f3fdde920956b7012dc835f33f6a1b8e9cacdd87ef54a2a`，切换前 quick_check 为 `ok`。
- 切换前后活动任务均为 `async_tasks=0`、`image_generations=0`、`video_generations=0`；切换后再次只读检查数据库 quick_check 为 `ok`，没有活动异步任务，也没有新生成任务。
- 切换后主服务为 `active`，PID `3911943`，`NRestarts=0`；本地 `/health` 与公网 `/drama-health` 均返回 200，切换后错误级服务日志为 0。
- 公网页面 `/`、`/login`、`/canvas/33`、`/script-analysis`、`/factory`、`/film/45` 均返回 200；公网首页与候选 `index.html` SHA-256 同为 `93071a3f99e8b1db21a216a38340d0af8e92743801cf6c6101b675807779c9e7`，两个入口静态资产均返回 200。
- AI 音乐保持隔离：`/opt/moli-mama` 的 server/worker PID 仍为 `206874`/`206895`，本地健康检查通过。
- 用户重新登录后，在同一登录态下完成三个板块的只读浏览器复核：`/script-analysis` 可读取既有验收项目、4 个分析版本、2 个角色、2 个场景、2 个道具和 7 个镜头，并实际切换角色/场景/道具页签；`/factory` 可读取现有项目并进入 `/drama/39`、`/film/39?episode=27`，制作页的脚本、角色、道具、场景、分镜、图片和视频区均已渲染，DOM 中 42 张图片与 8 个视频均可读取，8 个视频 `readyState=4`；没有触发保存、生成、审核或导出。
- `/canvas/63` 可读取 8 个自由节点（1 个文本、6 个图片、1 个视频）、现有结果图、参考素材和画布工具；实际打开 3D 导演台后，导演/机位视角切换、场景树、角色/道具入口、变换工具、相机与完整灯光预设均可用，未修改场景对象。该画布中的视频节点保留一条发布前已有的上游参数校验失败记录，它不是本次发布后新产生的服务错误，也不能据此把该供应商判定为当前可用。
- 登录态复核结束后再次只读检查生产：`current` 仍指向最终候选，主服务保持 `active`、PID `3911943`、`NRestarts=0`，本地和公网健康检查均为 200，切换后错误级服务日志仍无记录；AI 音乐 server/worker PID 仍为 `206874`/`206895`。浏览器已恢复到用户原先的 `/script-analysis` 页面。
- 本次发布没有调用付费供应商。以上证据不能扩张为“所有外部模型此刻均真实生成成功”，也不能证明未执行的生产写入操作没有缺陷。
- 系统盘切换后约 90% 使用、剩余约 18 GB；尚未删除候选、回滚版本或归档，后续容量治理仍须走独立受保护运维流程。

## 剩余覆盖付费验收准备

- 本地非付费门禁已在同一工作树重跑：前端 Node 测试 `677/677`，生产构建成功（`1858` 个模块），画布组合 Playwright `49 passed / 1 paid skipped / 0 failed`，独立画布运行闭环 `10/10`。跳过的付费供应商用例仍不计入可用性证明。
- 独立画布验收用例已跟随现行目录合同：模型必须出现在用户可访问的 `/canvas/model-catalog`；图片数量为 2 时提交两次生成，保留两条结果地址，只自动入库当前可见结果；视频首尾帧参考明确提交 `reference_mode=first_last`。本项只收紧测试证据，没有修改生产运行代码。
- 画布完整公开目录真实验收拟提交 `16` 次：7 个视频模型、7 个图片模型、1 个文本模型和 1 个音频模型，全部使用最低受支持参数，预计上限 `3671` 积分。
- 剧本分析拟建立隔离的单场景单镜头验收项目，执行分析、退回修订、通过并导入短剧工厂，预计 `2` 次文本生成、上限 `20` 积分。
- 短剧工厂拟在上述导入项目执行角色/场景/道具提示词、3 个资产图、1 个分镜图、1 个 480P/4 秒视频和 1 次配音，上限 `9` 次、`410` 积分；资产齐备后才运行一键流程，以避免该流程的自动重试产生额外付费提交。
- 计划硬上限为 `27` 次付费提交、`4101` 积分。未取得该精确次数与积分上限的单次授权前不执行；未知结果不自动重试，超出上限或需额外提交时立即停止。

### 2026-08-16 付费验收前置故障与修复

- 用户已批准最多 `27` 次、总计不超过 `4101` 积分的线上真实验收；开始账户基线为可用 `66536`、冻结 `240`。
- 首次付费提交前，线上独立画布图片节点的模型下拉渲染为 7 个空白选项，因此主动冻结付费验收；本阶段提交数仍为 `0/27`，实际消耗仍为 `0/4101`。
- 根因为增量候选混用了不兼容的组件契约：`DramaCanvas.vue` 向节点提供字符串数组，而线上 `HomeCanvasNode.vue` 按 `{ value, label }` 目录项渲染，导致 option 的值与标签均为空。
- 修复将节点模型选择统一为公开 `/canvas/model-catalog` 的目录项对象，并移除与“独立画布不读取管理员模型配置”合同冲突的旧回退；模型变化时按目录能力归一化清晰度和数量。
- 回归结果：前端 Node 测试 `677/677`，生产构建成功（`1858` 个模块），独立画布运行闭环 `10/10`，画布组合 Playwright `49 passed / 1 paid skipped / 0 failed`。付费跳过项仍不计入真实供应商证明。
