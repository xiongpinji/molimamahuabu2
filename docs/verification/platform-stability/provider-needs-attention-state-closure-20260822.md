# 供应商结果未知任务状态收口证据

## 只读生产复现

- 生产数据库只读检查发现 5 条图片任务长期保持 `processing`，对应线路请求均已是
  `needs_attention`，最新尝试均为 `submission_unknown`。
- 5 条请求均没有供应商任务号、结果 URL 或本地产物路径，不能安全查询供应商终态，
  也不得自动重试或退款。
- 这 5 条任务各冻结 40 积分，修复合同要求预扣继续保持 `held`，等待管理员核对。
- 根因是对账器已写入 `needs_attention` 事件后直接跳过，且旧逻辑把关联任务重新写为
  `processing`，导致数据库状态无法收敛。

## 精准修复

- 对结果未知且不可安全恢复的请求，将关联异步任务及图片/视频记录收口为
  `needs_attention`，不再伪装为仍在生成。
- 即使提醒事件已存在，只要关联记录仍是遗留状态，仍执行一次状态收口；全部收口后重复
  对账不再产生变化或重复事件。
- 不调用供应商、不创建新任务、不自动退款；原积分预扣保持冻结。

## 本地 TDD 证据

- 红灯：新增生产形态回归后，既有事件导致对账结果为 0，关联任务仍为 `processing`。
- 绿灯：`providerReconciliation.test.js` 6/6 通过。
- 相邻账务、任务、图片路由与视频路由回归合计 49/49 通过。
- 后端全量：2664 项，2654 通过、0 失败、10 跳过，退出码 0。
- 功能锁审计：`ready=true`；精确发布范围测试通过。
- 独立代码审查：8 个文件，0 个 Critical、Important 或 Minor 问题，结论可提交。
- `node --check` 与 `git diff --check` 通过。

## 生产边界

本文件只记录只读诊断与本地验证。尚未执行生产数据库写入、生产候选激活、供应商付费
调用或 AI 音乐相关操作。生产遗留任务收口必须先备份数据库，并在新的明确授权下执行。
