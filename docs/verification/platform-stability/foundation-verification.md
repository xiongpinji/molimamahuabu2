# 平台稳定性底座验收证据

## 验收范围

- 日期：2026-08-15
- 分支：`codex/platform-stability-one-pass-lock-20260815`
- 基线：`8f9a66cd708d5db96fbee573a2f4aa7de182a6fe`
- 范围：图片、视频、文本共享供应商路由；未知任务与积分保护；管理员稳定性日志和中转关联；功能锁与增量发布范围门禁。
- 明确不在本阶段锁定：画布、短剧工厂、剧本分析的完整业务功能。三者必须在底座合入并发布回读后，各自从最新 `main` 进入独立的一次性验收阶段。

## 红灯与精准修复

后端首次全量为 843 项，其中 2 项失败：分镜图片本地集成配置和公开视频模型测试夹具仍依赖迁移前的隐式可用状态。隔离复现确认生产规则正确，未放宽“只有真实验证配置可进入目录和路由”的门禁；只将本地测试夹具显式标记为 `verified` 并记录测试证据。两项隔离测试随后分别通过。

Playwright 首轮为 14 通过、1 失败、1 未运行。失败项的本地图片工具能力夹具同样缺少 `verified` 状态，导致“背景重构”按生产规则禁用。只补齐本地能力夹具的验证证据后，该用例单独通过，完整两组浏览器回归通过。

## 最终无付费验证

| 检查 | 命令 | 结果 |
| --- | --- | --- |
| 后端全量 | `cd backend-node && npm test` | 843 项；842 通过、1 跳过、0 失败 |
| 功能锁清单 | `cd backend-node && npm run audit:feature-lock` | 退出码 0；4 个共享稳定性功能有效 |
| 增量发布合同 | `cd backend-node && node --test --test-concurrency=1 test/incrementalReleaseScope.test.js` | 4/4 通过 |
| 前端全量 | `cd frontweb && node --test test/*.test.js` | 647/647 通过 |
| 前端生产构建 | `cd frontweb && npm run build` | 退出码 0；仅有既有大 chunk 警告 |
| 本地浏览器回归 | `cd frontweb && npx playwright test e2e/project-canvas-ci.spec.js e2e/image-node-toolbar-backend-integration.spec.js` | 15 通过、1 跳过、0 失败 |

浏览器回归显式设置 `RUN_REAL_PROVIDER_TESTS=0` 和 `ALLOW_PAID_GENERATION=0`。跳过项是需要单次付费授权的真实 AIHubCC 图片工具链；本阶段没有发起真实供应商生成，没有消耗用户积分或供应商余额。

## 锁定提交

- 已验证同模型供应商池：`9fe8285f737fabe57078f6615c72223b4b5b9e14`
- 图片安全切换：`4702a275`
- 视频安全切换与固定任务：`4cf4a24d`
- 文本流式未知态保护：`73186d44b427b8dac5083853f1ec10b1d7252905`
- 重启与积分对账保护：`3c3640667c5afd6f55216ec0a82acc0f0550fd9f`
- 管理员稳定性面板：`f960703ce43e67900b6a294e61d36cf40f2a1d1e`
- 功能锁和增量范围门禁：`d23bfd19`

## 安全与交付边界

- 管理员可以查看模型对应中转站、健康、切换、任务和积分状态；普通用户接口与页面不返回中转站、配置 ID、成本、密钥或内部错误。
- 只有明确未受理且同逻辑模型、同能力、已验证的供应商允许自动切换。内容安全、参数错误、已受理、结果未知和不可读产物均不自动重提。
- 结果未知时任务进入人工核对，积分保持冻结；只有明确失败且没有供应商任务时退款。
- 本轮未访问生产 SSH，未创建生产候选，未推送分支，未创建 PR，未部署，也未触碰 AI 音乐服务。
- 真实生产候选的增量范围审计必须在后续从实时 `/opt/moli-drama/current` 克隆候选后执行；本地阶段没有伪造该证据。
