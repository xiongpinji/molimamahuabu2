# 供应商任务实时候选兼容修复验证

## 范围

- 从操作时实时 `/opt/moli-drama/current` 克隆候选。
- 只修复迁移 SQL 触发器分句、视频逻辑模型身份匹配、不可读视频产物转 `needs_attention` 三处已确认漂移。
- 任一源码不符合已批准旧形态或已修复形态时失败退出，不继续制作候选。
- 不覆盖整文件，不修改生产数据库，不请求供应商，不付费，不触碰 AI 音乐。

## 本地 TDD 证据

- 候选实现提交：`5cc0dd4d0abcb91581e6b127f758f4e74222bc6f`，验证时间：`2026-08-23 08:53:20 +08:00`。
- 初始红灯：功能锁、发布范围和兼容补丁测试共 10 项，3 项通过、7 项因规则缺失失败；实际旧候选回放又发现 `videoClient` 相邻函数边界不同，新增用例先以 0/1 失败复现。
- 聚焦绿灯：`providerTaskLiveCompatibility`、`incrementalReleaseScope`、`featureLockManifest` 共 37/37 通过。
- 实际旧候选回放：首次只读检查退出 2，并仅报告 `videoClient.js`、`videoService.js` 两处待修；应用退出 0；再次检查退出 0 且零待修。该快照的迁移执行器已正确，因此没有被重复改写。
- 供应商任务、计费、图片/视频路由相邻回归共 372/372 通过。
- 后端全量：36 个套件、2892 项，2882 通过、0 失败、10 跳过，退出 0。
- 功能锁审计：`ready=true`，6 个功能锁，`origin/main...5cc0dd4d` 共 7 个实际变更路径。
- 范围审计：实际 7 路径与十文件白名单扣除三处候选运行时修复路径后逐项相等；运行数据、用户资产、AI 音乐和 shared release guard 均不在范围内。
- 语法、JSON、`git diff --check` 均通过；新增行未命中 Key、Bearer 长令牌或 URL userinfo 形态。
- 本地当前源码执行 `--check` 为 `ready=true` 且零待修，证明补丁对已正确候选幂等。

## 未执行门禁

本文档不代表生产候选已制作、已激活或生产功能已验收。推送与 Hosted CI 由本轮后续步骤验证；SSH 候选制作和激活仍需分别受保护执行，其中激活未获授权。
