# 供应商运行中请求对账竞态修复证据

## 生产复现

- 剧本分析自动修订在供应商请求仍正常运行时，被 60 秒定时对账提前写成
  `submission_unknown`；原请求随后成功返回，未发生重复提交。
- 短剧工厂角色图片生成 `#556` 仅调用主路由一次并成功生成可读本地产物，
  但旧版本仍在任务、图片记录和路由尝试中保留“结果未知”提示。
- 图片路由候选为同一逻辑模型的三个已验证配置；本次生产请求未触发备路由。

以上两次复现都证明问题发生在平台对账时序，不是供应商失败。未知结果不得自动
重试、积分保持冻结、明确未受理才允许同模型切换的合同继续保持不变。

## 精准修复

- 正常进程内的 `submitting` 请求增加 20 分钟等待窗口，覆盖图片同步调用及文字
  模型长响应；窗口内定时对账不改写任务状态。
- 服务启动恢复仍以零等待窗口处理遗留 `submitting` 请求，因为原进程调用已经
  无法恢复，继续进入人工核对并保持积分冻结。
- 供应商产物验证成功后，统一清除路由尝试、异步任务、图片记录及角色/场景上的
  历史错误，避免正确结果继续向用户展示失败提示。

## 验证

- 修复提交：`51a91f1e3eb5dc3636adb1c8a47e45f22a60e000`
- 后端全量：858 通过，1 跳过，0 失败。
- 新增回归覆盖：等待窗口内不误判、陈旧提交仍进入核对、成功终态清理旧错误、
  启动恢复保持原保护语义。
- `node --check`：5 个修改服务文件全部通过。
- `git diff --check`：通过。

本文件不记录中转密钥、完整供应商响应或用户提示词。
