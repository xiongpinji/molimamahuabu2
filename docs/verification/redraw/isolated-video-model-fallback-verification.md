# PR #217 整集转绘隔离多视频模型回退验证

## 授权与边界

- 产品主授权：`product-owner 2026-09-05 pr217-isolated-video-model-fallback`。
- 开发基线：PR #217 HEAD `823808b5f307c4d54e21eb63a881192c4ad2b90f`。
- 范围：Fumin Fast → ToAPIs Fast → ToAPIs Wan3 → 飞拓 Seedance 2.5，只对 `shot-01.part-01` 竞选，首个通过者独占剩余 27 个单元。
- 硬门禁：明确供应商失败才允许切换；任一未知或产物验收失败全局停止；整轮最多 31 次生成提交。
- 不合并、不部署、不读写生产数据库、不修改线上模型；Key 不进入源码、状态、日志或证据。

## 当前实现证据

- 固定路由注册表已锁定四条线路、精确模型和 480p / 9:16 / 5 秒合同。
- Fumin 保留 Mini 默认行为，只允许本地隔离运行器显式注入 Fast。
- ToAPIs Fast、ToAPIs Wan3 和飞拓 Seedance 2.5 复用现有后端供应商客户端；终态分类只在新建隔离适配层完成，不改动受外部证据保护的客户端。媒体、音轨、双 ASR、对白、身份参考绑定、裁剪和整集合成复用现有整集验收链。
- 已移除历史上把“身份参考已绑定”直接记录成 `role.passed=true` 的虚假通过；有角色的成片改为 `pending_external_review`，最终交付前必须对接触表执行独立视觉一致性复核。
- 父调度器使用路由子状态和原子父 manifest，在每个生成 POST 前记录唯一尝试并执行 31 次硬上限。
- 供应商失败原因经脱敏后持久化，不再只保存通用错误码。

## 本地 TDD 证据

| 门禁 | 结果 |
| --- | --- |
| 固定四路注册表 | 3/3 通过 |
| 四线路请求/终态适配 | 5/5 通过 |
| Fumin 适配器与媒体链 | 31/31 通过 |
| 通用整集运行器 | 34 通过，2 项 Windows 无权创建 symlink 跳过 |
| 多模型父调度器 | 10/10 通过，包含真实通用运行器 28 单元零供应商端到端、真实 CLI HEAD 绑定和空目录复用拒绝 |
| 上述定向组合回归 | 83 通过，0 失败，2 跳过 |
| 既有单模型整集回归 | 71 通过，0 失败，2 跳过 |
| 功能锁与发布范围 | 66/66 通过 |
| 共享外部模型发布门禁 | 147 通过，0 失败，3 跳过 |
| 前端生产构建 | 通过（1921 modules；仅保留既有 chunk-size 警告） |

首次全量后端回归曾捕获一次对受保护 ToAPIs 客户端的非必要改动，并因共享证据哈希不匹配失败；该客户端及其测试已精确恢复到基线，终态分类移入本次新建的隔离适配层，原失败测试文件随后独立重跑为 147 通过、0 失败、3 跳过。首次全量回归发生在修复前，不能冒充当前精确 HEAD 的全绿证据；精确提交后的 Hosted CI 是最终全量门禁。

## Hosted CI 与真实生成状态

- Hosted CI：待新 HEAD 推送后运行；必须精确 HEAD 4/4 全绿，未通过前不得读取 Key 或调用供应商。
- 真实生成：当前 0 上传、0 生成 POST、0 新增扣费。
- 整集产物：待精确新 HEAD Hosted CI 4/4 全绿后，按授权在全新本地隔离状态中执行。
