# 建议修复与后续事项

## 本次发布前

没有阻断性产品修复。继续执行以下发布门禁：

1. 获取最新 `origin/main`，确认本分支仍只包含导演台范围差异。
2. 重跑定向测试、差异检查和生产构建。
3. PR 全部门禁通过后再合并。
4. 部署前执行生产数据备份，使用原子版本切换。
5. 线上回归导演台打开、三点布光、灯光选择与机位角度保存。

## 非阻断后续

- 为后续像素级视觉调整建立独立视觉规格和设计系统评审文件。
- Playwright 多工作树并行运行时固定使用独立端口并关闭 `reuseExistingServer`，避免测试命中旧服务。
- 前端完整 Node 测试中的 6 项既有画布源码契约失败应另立任务处理，不并入本导演台包。
