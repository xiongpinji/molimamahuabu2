# 画布交互与生成可靠性收口

## 范围

本任务只修改 `/canvas/:id` 使用的画布交互、生成可靠性、导演台和真实协作入口。
不修改另一个会话负责的图片节点工具栏。

## 验收标准

1. 焦点不在输入框或编辑器时，`W/A/S/D` 可连续平移画布。
2. 节点右键菜单提供“创建副本”，副本保留配置但使用新 ID 和偏移位置。
3. 连线可拖到目标节点主体完成连接，不要求精确命中小加号。
4. 视频节点新建后沿用用户最近选择的比例、时长和清晰度。
5. `@图片` 仅列出通过边直接连接到当前节点的上游图片节点。
6. 每个节点保存最近生成记录，包含状态、时间、结果或失败原因。
7. 视频成功后保存首帧与尾帧地址；失败扣费必须幂等返还。
8. 导演台至少提供女性角色预设，并允许相机和角色自由旋转。
9. 协作入口使用真实工作区成员接口；不把“复制链接”冒充协作。

## 验证

- 前端定向单元测试：39/39 通过。
- 后端视频边界帧、重复任务和计费测试：11/11 通过。
- 独立端口 Playwright 画布回归：28/28 通过。
- 前端生产构建：通过（保留既有大分块告警）。
- 前端与后端 `npm audit --audit-level=high`：均为 0 个漏洞。
- 只选择性提交本任务文件并更新现有 PR。
