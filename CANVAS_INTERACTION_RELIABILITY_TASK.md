# 画布交互与生成可靠性收口

## 范围

本任务只修改 `/canvas/:id` 使用的画布交互、生成可靠性、导演台和真实协作入口。
不修改另一个会话负责的图片节点工具栏。

## 2026-07-30 回归修复

生产页实测确认：

- `libtv` 类型连线只渲染底线和流光，缺少断线按钮。
- 节点编辑器默认留在 Vue Flow 变换层内，会随画布缩放并出现显示不完整。
- 参考图卡片仍显示用途、排序、权重和启用选项。

本轮验收：

1. `libtv` 连线悬停或选中时显示“剪断连线”按钮，并调用现有断线能力。
2. 节点编辑器挂载到视口，普通和全屏状态都不受画布缩放、裁切影响；普通状态按节点位置停靠到空余侧，不遮挡节点工具栏和媒体双击预览。
3. 参考图卡片只保留序号、预览、名称和移除按钮，不显示用途、排序、权重、启用选项。

本轮验证：

- 四项定向回归测试：4/4 通过。
- 独立端口 Playwright 完整画布回归：36/36 通过；1 条需显式付费供应商凭据的真实 AIHubCC 用例按设计跳过。
- 前端生产构建：通过（保留既有大分块告警）。
- Chrome 本地实测：画布缩放到 277% 时，普通编辑框仍挂载在 `BODY`，不再随画布缩放；全屏编辑框距视口四边 16px。
- 全量前端测试：438/441 通过；节点拖拽缓存、空白右键和旧“镜头运动”命名这 3 条失败在纯 `origin/main` 上可复现，不由本轮改动引入。

## 验收标准

1. 焦点不在输入框或编辑器时，`W/A/S/D` 可连续平移画布。
2. 节点右键菜单提供“创建副本”，副本保留配置但使用新 ID 和偏移位置。
3. 连线可拖到目标节点主体完成连接，不要求精确命中小加号。
4. 视频节点新建后沿用用户最近选择的比例、时长和清晰度。
5. `@图片` 仅列出通过边直接连接到当前节点的上游图片节点。
6. 每个节点保存最近生成记录，包含状态、时间、结果或失败原因。
7. 视频成功后保存首帧与尾帧地址；失败扣费必须幂等返还。
8. 导演台至少提供女性角色预设，并允许相机和角色自由旋转。
9. 协作入口使用真实工作区成员接口；不把“复制链接”冒充协作。

## 验证

- 前端定向单元测试：39/39 通过。
- 后端视频边界帧、重复任务和计费测试：11/11 通过。
- 独立端口 Playwright 画布回归：28/28 通过。
- 前端生产构建：通过（保留既有大分块告警）。
- 前端与后端 `npm audit --audit-level=high`：均为 0 个漏洞。
- 只选择性提交本任务文件并更新现有 PR。
